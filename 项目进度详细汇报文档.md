# 邮件报修自动化系统 — 项目进度详细汇报文档

**报告日期：** 2026-07-10  
**项目代号：** `repair-mail-agent`  
**运行状态：** 🟢 本地已可运行（SSH隧道连接远程MySQL，前后端正常启动，admin/admin123可登录）

---

## 一、项目进度总览

### 核心指标

| 指标 | 数值 | 状态 |
|------|------|------|
| 项目整体完成度 | 82% | 🟡 |
| 后端 API | 95%（67端点，1骨架） | 🟢 |
| 前端页面 | 92%（15页面） | 🟢 |
| 数据库 | 90%（26表完整） | 🟢 |
| 核心业务流程 | 85% | 🟡 |
| 外部服务集成 | 25%（仅AI+SMTP） | 🔴 |
| 测试覆盖 | 40%（49用例） | 🔴 |
| 部署配置 | 50% | 🟡 |

### 已完成 vs 待完成

**✅ 已完成：** 67个API端点、15个前端页面、26张数据库表、8状态×31转换规则、AI解析流水线、人工复核闭环、回复审核闭环(SMTP)、RBAC权限体系、SN校验+资产匹配

**❌ 待完成（P1）：** IMAP客户端、OSS客户端、SSE实时推送、APScheduler调度、ORM Relationship定义

---

## 二、项目目录结构

```
repair-mail-agent/
├── backend/                        # Python/FastAPI
│   ├── app/
│   │   ├── main.py                 # FastAPI入口，lifespan/CORS
│   │   ├── config.py               # Pydantic Settings（8类配置）
│   │   ├── seed.py                 # 种子数据
│   │   ├── api/v1/                 # REST API（13模块，67端点）
│   │   │   ├── router.py / auth.py / users.py / dashboard.py
│   │   │   ├── emails.py / email_threads.py / tickets.py
│   │   │   ├── parse_results.py / manual_review.py / replies.py
│   │   │   ├── master_data.py / ai_logs.py / notifications.py
│   │   │   ├── system.py / statistics.py / db_browser.py
│   │   ├── core/                   # database/security/errors/response
│   │   ├── models/                 # 10文件，26表ORM
│   │   │   ├── identity.py / mail.py / tickets.py / workflow.py
│   │   │   ├── parsing.py / master_data.py / replies.py
│   │   │   ├── review.py / storage.py / logs.py / base.py
│   │   ├── services/              # 12业务服务模块
│   │   │   ├── common.py / ai.py / audit.py / emails.py
│   │   │   ├── manual_review.py / master_data.py / parser.py
│   │   │   ├── replies.py / tickets.py / users.py / workflow.py
│   │   │   └── runtime_config.py
│   │   ├── integrations/          # ai_provider.py(DeepSeek)
│   │   ├── repositories/          # 空（一期不实现）
│   │   └── workers/               # 空（APScheduler待初始化）
│   ├── alembic/versions/          # 2个迁移文件
│   ├── tests/                     # 7文件，49用例
│   └── requirements.txt           # 17个依赖
│
├── frontend/                      # React/Vite
│   ├── src/
│   │   ├── pages/                 # 15页面（Login/Dashboard/Emails/Tickets
│   │   │   │                     #  ManualReview/Replies/MasterData/Users
│   │   │   │                     #  Profile/AILogs/System/Statistics/
│   │   │   │                     #  Notifications/DbBrowser/NotificationCenter）
│   │   ├── components/            # StatusTag/SectionPanel/JsonBlock/PageTitle
│   │   ├── api/client.ts          # 48个API方法
│   │   ├── stores/authStore.ts    # Zustand认证状态
│   │   └── utils/                 # download/filters/format/roles/status
│   └── vite.config.ts             # 端口5173，/api代理→8000
│
├── docker/                        # backend.Dockerfile + frontend.Dockerfile
├── nginx/default.conf             # 反向代理
├── docker-compose.yml             # mysql/backend-api/frontend/nginx
├── .github/workflows/deploy.yml   # CI/CD
├── deploy.sh / tunnel.py / .env
└── docs/                          # 项目文档
```

---

## 三、当前业务流程（代码实现） vs 四、目标业务流程（需求定义）

### 当前流程（10步）

```
① 手工入库 → ② 去重归并 → ③ 分类解析 → ④ AI解析 → ⑤ 生成工单
→ ⑥ SN校验 → ⑦ 追问草稿 → ⑧ 人工复核 → ⑨ 可导出 → ⑩ 关闭（仅人工）
```

### 目标流程（10步）

```
① 批量捞取 → ② 去重合并 → ③ 分类解析 → ④ 多模态附件处理 → ⑤ 全量AI解析
→ ⑥ 生成工单 → ⑦ SN校验 → ⑧ AI追问≤3轮 → ⑨ 客户确认到货 → ⑩ 工单结束
```

### 核心差异对比

| # | 节点 | 当前 | 目标 | 差距 |
|---|------|------|------|------|
| 1 | 邮件获取 | 手工API入库 | IMAP批量全量同步 | 🔴 |
| 4 | 附件处理 | 仅保存元数据 | 多模态AI+不确定转人工 | 🔴 |
| 5 | AI解析 | 纯文本+置信度 | +附件内容+置信度低原因 | 🟡 |
| 9 | 可导出 | 仅人工转入 | 客户确认到货自动触发 | 🔴 |
| 10 | 闭环 | 仅人工关闭 | 客户确认自动闭环+回复链边界 | 🔴 |

---

## 五、工单状态流转

### 状态码定义

| 状态码 | 中文 | 类别 | 终态 |
|--------|------|------|------|
| `new_email` | 邮件已入库 | intake | - |
| `parsed` | 已解析 | processing | - |
| `need_customer_info` | 待客户补充 | waiting | - |
| `auto_replied` | 已自动回复 | waiting | - |
| `manual_review` | 人工复核 | review | - |
| `ready_for_export` | 可导出 | export | - |
| `error` | 异常 | error | - |
| `closed` | 已关闭 | terminal | **是** |

### 核心流转路径

```
邮件入库 → reparse_email
  ├─ AI完美 → new_email → parsed → [need_customer_info → auto_replied → (客户回复)→ parsed] → ready_for_export → closed
  ├─ AI低置信 → new_email → manual_review → (人工resolve) → ready_for_export/need_customer_info/closed
  ├─ AI需人工 → manual_review（直接） → (人工resolve) → ...
  └─ AI不可用 → manual_review（用规则解析） → (人工resolve) → ...
```

### 12条基础转换规则

| # | from → to | trigger_event |
|---|-----------|--------------|
| 1 | new_email → parsed | parse_completed |
| 2 | new_email → manual_review | parse_low_confidence |
| 3 | parsed → ready_for_export | validation_passed |
| 4 | parsed → need_customer_info | missing_fields_detected |
| 5 | parsed → manual_review | field_conflict |
| 6 | need_customer_info → auto_replied | reply_draft_created |
| 7 | auto_replied → parsed | customer_reply_received |
| 8 | manual_review → ready_for_export | manual_resolved |
| 9 | manual_review → parsed | manual_resolved |
| 10 | manual_review → need_customer_info | manual_resolved |
| 11 | error → parsed | manual_resolved |
| 12 | ready_for_export → closed | export_completed |

（另有自动扩充的17条A/B/C类通用规则，共29条去重后）

---

## 六、关键业务详细信息

### 6.1 批量捞取与去重合并

| 级别 | 置信度 | 匹配策略 |
|------|--------|---------|
| L1 | 0.95 | In-Reply-To精确命中 |

### 6.2 邮件意图分类

| intent_type | 置信度 | 条件 | 后续 |
|-------------|--------|------|------|
| customer_reply | 0.85 | 有In-Reply-To | 关联已有工单 |
| new_repair | 0.80 | 命中报修/维修/rma/sn | 创建新工单 |
| internal_forward | 0.65 | fwd/fw/转发 | 可能忽略 |
| irrelevant | 0.90 | 退订/广告 | 跳过 |
| unknown | 0.45 | 都不命中 | 转人工 |

### 6.3 附件处理
🔴 **未实现** — 当前仅保存元数据到email_attachments，不参与AI解析

### 6.4 AI解析
🟡 基础完成 — DeepSeek API → Pydantic JSON校验 → 质量增强 → 记录AiCallLog+JSONL
❌ 缺：附件内容注入、置信度低原因文字

### 6.5 工单生成
🟢 已完成 — ticket_no=RMA+时间戳，自动字段填充，两条入口（自动/人工）

### 6.6 SN校验 → 见第八章
### 6.7 AI追问 → 见第十章
### 6.8 客户确认 → 🔴 未实现
### 6.9 闭环 → 🔴 仅人工关闭

---

## 七、不同业务分支工单流转

| 分支 | 入口条件 | 初始状态 | 关键路径 |
|------|---------|---------|---------|
| A：AI完美 | 置信度≥0.7，无缺失/冲突 | new_email | →parsed→need_customer→auto_replied→ready→closed |
| B：AI低置信 | 置信度<0.7 | new_email→manual_review | 人工resolve→6种next_action |
| C：AI需人工 | 意图不明/有缺失/冲突 | manual_review（直接） | 同B |
| D：AI不可用 | AI异常 | manual_review（规则解析） | 同B |
| E：追问超限 | followup≥3 | 转入manual_review | 强制人工介入 |
| F：SN异常 | SN校验failed/warning | 转入manual_review | priority=high |

---

## 八、SN校验规则

### 校验步骤

```
逐明细行校验SN:
  sn为空? → failed("缺少SN")
  sn_assets未找到? → failed("SN不存在于资产库")
  asset_status≠valid? → failed("SN状态异常")
  [资产valid] 客户匹配?物料匹配? → warning/pass
  + 查询board_cards获取北京发货规则

状态流转(仅parsed时):
  has_problem/has_warning → manual_review(field_conflict, priority=high)
  全部pass+无missing → ready_for_export(validation_passed)
```

### 校验结果状态

| 结果 | 条件 | 状态影响 |
|------|------|---------|
| failed | 缺SN/无资产/非valid | → manual_review |
| warning | 客户不匹配/物料不匹配 | → manual_review |
| pass | 全部通过 | 可→ ready_for_export |

### SnValidationResult快照字段
check_exists / check_valid / check_customer_match / check_material_match / need_ship_to_beijing / result_status / result_message

---

## 九、回复链识别与工单生成

### 三级匹配策略

| 级别 | 置信度 | 策略 |
|------|--------|------|
| L1 | 0.95 | In-Reply-To → 查已有Email.message_id |
| L2 | 0.65 | 归一化主题(ex. "Re:回复:设备故障"→"设备故障") → 查EmailThread.normalized_subject |
| L3 | 1.00 | 新建线程 → thread_key=SHA256(message_id:subject) |

### 线程→工单关联
Email → EmailThread(ticket_id) → RepairTicket（三级查找优先级: ParseResult.ticket_id → thread.ticket_id → EmailTicketLink）

核心规则：**一个EmailThread通常对应一个RepairTicket。**

---

## 十、自动追问规则

### 追问流程

```
create_reply_draft:
  前置: 追问次数≥3 → 转入manual_review(priority=high) → 抛异常
  模板: missing_info_zh / sn_invalid_zh / manual_review_notice_zh
  渲染: 模板 → generate_ai_reply_draft(AI润色) → ReplyRecord
  发送: _reply_can_auto_send判断 → STARTTLS(587) → 归档出站
  流转: need_customer_info → auto_replied(reply_draft_created)
```

配置: MAX_FOLLOW_UP=3, AUTO_SEND_ENABLED=false, AUTO_SEND_MIN_CONFIDENCE=0.85

---

## 十一、人工处理任务推送与分配

### 任务生命周期
pending → assigned/claimed → (release)→ pending → (resolve)→ resolved（终态）

### 任务类型

| task_type | 触发 | 优先级 |
|-----------|------|--------|
| parse_low_confidence / manual / ai_review_required | AI相关 | normal |
| field_conflict / followup_limit / reply_send_failed / system_error | 异常 | **high** |
| reply_review | 草稿需确认 | normal |

### 通知机制
创建任务: manual_review_created(全局广播) + manual_review_assigned(定向)
分配: manual_review_assigned(定向)
认领/释放/解决: 无通知

### resolve_task 6种 next_action
transition_ready_for_export / wait_customer_info / generate_followup(+追问)/ reparse(+重解析)/ close_ticket / keep_manual_review

---

## 十二、数据库表（26张）

```
users ─┬─ user_roles ── roles
       ├─ repair_tickets(assigned) / manual_review_tasks(assigned/claimed/resolved)
       ├─ operation_logs / notification_events(recipient) / parse_results(applied/accepted)
       └─ (15处FK)

email_threads ─┬─ emails ─┬─ email_attachments
               │           ├─ email_ticket_links ── repair_tickets
               │           ├─ parse_results / reply_records / manual_review_tasks
               └─ repair_tickets(thread_id)
                  repair_tickets ─┬─ repair_ticket_items ── sn_validation_results
                                  ├─ ticket_status_logs / field_audit_logs
                                  ├─ parse_results / reply_records / manual_review_tasks

workflow_statuses ── workflow_transitions(from/to) / ticket_status_logs(from/to)
sn_assets ── repair_ticket_items(sn_asset) / sn_validation_results(matched_asset)
board_cards(material_code唯一) ← sn_assets.material_code
reply_templates ── reply_records(template)
oss_objects / ai_call_logs / system_event_logs / job_run_logs
```

---

## 十三、后端功能详情

| 模块 | API前缀 | 端点 | Service | 相关表 |
|------|---------|------|---------|--------|
| 认证 | /auth | 4 | users.py | users,roles |
| 用户管理 | /users | 7 | users.py | users,roles,user_roles |
| 邮件中心 | /emails | 4 | emails.py | emails,threads,attachments |
| 邮件线程 | /email-threads | 2 | emails.py | email_threads,emails |
| 工单中心 | /tickets | 13 | tickets.py | repair_tickets,items,status_logs |
| 解析结果 | /parse-results | 1 | tickets.py | parse_results |
| 人工复核 | /manual-review | 7 | manual_review.py | manual_review_tasks |
| 回复审核 | /replies | 5 | replies.py | reply_records,templates |
| 基础资料 | /master-data | 12 | master_data.py | sn_assets,board_cards |
| AI日志 | /ai-logs | 1 | (endpoint直接) | ai_call_logs |
| 通知 | /notifications | 3 | audit.py | notification_events |
| 系统 | /system | 7 | runtime_config.py | workflow_statuses/transitions |
| 看板 | /dashboard | 1 | (endpoint直接) | emails,tickets,tasks |
| 工作流引擎 | — | — | workflow.py | workflow_statuses/transitions,tasks |

---

## 十四、前端页面详情

| 页面 | 路由 | 行数 | API调用 | 功能 |
|------|------|------|---------|------|
| Login | /login | 43 | POST /auth/login | 登录 |
| Dashboard | / | 86 | GET /dashboard/summary | 8指标+异常任务 |
| EmailsPage | /emails | 225 | 4个 | 邮件列表/入库/详情/重解析 |
| TicketsPage | /tickets | 812 ⚠️ | 10个 | 工单全生命周期 |
| ManualReviewPage | /manual-review | 911 ⚠️ | 12个 | 三栏工作台 |
| RepliesPage | /replies | 170 | 4个 | 草稿/审核/发送 |
| MasterDataPage | /master-data | 290 | 8个 | SN/板卡导入导出 |
| UsersPage | /users | 304 | 7个 | 用户CRUD+角色 |
| ProfilePage | /profile | 91 | 3个 | 个人资料/密码 |
| AILogsPage | /ai-logs | 136 | 1个 | AI日志 |
| SystemPage | /system | 347 | 7个 | 配置/模板/状态 |
| StatisticsPage | /statistics | 148 | 1个 | 时段统计 |
| NotificationsPage | /notifications | 150 | 2个 | 消息管理 |
| NotificationCenterPage | /notification-center | 120 | 2个 | 快速预览 |
| DbBrowser | /db-browser | 151 | 2个 | 表结构/数据预览 |

⚠️ TicketsPage和ManualReviewPage超过800行，建议拆分内联子组件
