# 邮件报修自动化系统统一开发总结文档

生成日期：2026-07-03

## 当前实现基线（2026-07-05）

本文件保留统一开发方向和历史设计总结。当前代码事实以 README、AI 开发进度文档和 `docs/alignment-audit/` 为准：

- 基础业务、DeepSeek AI、用户管理、个人信息、站内消息和接口级 RBAC 已完成首轮开发。
- 数据库为 26 张业务表 + `alembic_version`，最新迁移 `9d2b7c4f1a30`。
- `parse_results.apply_status/applied_by_user_id/applied_at` 已落地。
- 标准角色为 `admin/supervisor/operator`，旧 `viewer` 方向废弃。
- 远程新库 `repair_system_codex_dev_test` 已验证，保留临时管理员 `codex_admin_validation`。
- 真实 IMAP/SMTP/OSS 未联调，自动发送默认关闭。

本文件用于归并当前目录下与 `repair-mail-agent` 项目相关的 PRD、详细设计、增强设计、数据库设计、部署流程、CI/CD 标准和一致性审查结论。后续开发优先以本文作为统一入口；原文档不删除，作为细节追溯、历史记录和字段级参考。

---

## 1. 文档定位与使用规则

### 1.1 统一入口

后续开发、联调、测试、部署和 AI 编程助手执行任务前，优先阅读本文。本文解决以下问题：

- 合并重复的研发标准、Docker、Nginx、GitHub Actions、远程部署流程。
- 将增强版详细开发文档中的前置分类、人工任务包、复杂回复链、多邮件多附件、字段证据等业务细节纳入统一设计。
- 将数据库设计文档中的 26 张表归纳为关键表和关键字段，不在本文重复所有字段细节。
- 保留旧文档的查阅入口，避免因删除历史资料导致细节丢失。

### 1.2 冲突优先级

当旧文档、代码和本文不一致时，按以下优先级判断：

1. 本文的统一口径。
2. `repair-mail-agent/AI开发进度与任务跟踪.md` 和 `repair-mail-agent/README.md` 中记录的当前真实项目状态。
3. `文档一致性审查报告.md` 的冲突裁定与问题清单。
4. `邮件报修自动化系统PRD技术方案.md` 的业务目标和一期边界。
5. `邮件报修自动化系统详细开发设计文档_增强版.md` 的功能细节。
6. `邮件报修自动化系统数据库表字段设计方案_一期最终版.md` 的全字段、索引和关系。
7. `Codex_Docker_CICD_标准研发与部署流程.md`、`远程服务器Docker容器化开发部署执行顺序说明.md`、`codex-git-01.md` 作为历史过程和专项细节参考。

### 1.3 仍需查阅旧文档的场景

- 要看某张表的完整字段、索引、外键：查数据库字段设计文档。
- 要看业务需求原始描述、一期范围、验收样例：查 PRD。
- 要看接口列表和页面细节的原始扩展描述：查增强版详细开发文档。
- 要看已发现的文档冲突、风险编号、修订建议：查一致性审查报告。
- 要看远程服务器、GitHub、deploy key 的历史验证记录：查 `codex-git-01.md` 和项目内 `docs/remote-mysql-root-deployment.md`。

---

## 2. 开发目标与一期边界

### 2.1 项目定位

邮件报修自动化系统是一套面向内部试运行的邮件报修处理控制台。系统通过邮件归档、回复链归并、规则/附件/AI 解析、SN 校验、追问草稿、人工复核和工单状态机，实现客户邮件报修从收信到可导出的闭环沉淀与可追溯管理。

### 2.2 一期核心目标

- 自动接收报修邮箱邮件，完成 Message-ID 去重、邮件元数据入库、原始 EML 和附件 OSS 归档。
- 识别新报修、客户补充、内部转发、无关邮件和未知邮件。
- 从正文、标准模板、附件和 AI 候选中抽取结构化报修信息。
- 通过 SN/资产库和板卡/物料规则校验 SN、客户、物料和寄送规则。
- 缺少关键信息时生成追问草稿，默认进入人工审核。
- 低置信度、SN 异常、字段冲突、追问超限、线程不确定、系统异常必须进入人工复核。
- 所有工单状态、字段变更、AI 调用、后台任务和人工动作可追溯。
- 前端提供邮件中心、工单中心、人工复核、自动回复审核、基础资料、AI 日志、系统配置和看板。

### 2.3 一期不做

- 不做正式生产高可用、多租户、复杂权限矩阵和外部系统深度集成。
- 不直接接入 SAP/ITSM，下游导出先以 `ready_for_export` 状态作为边界。
- 不引入 Redis、Celery、Prometheus、Node Exporter。
- 不默认真实自动发送邮件，`AUTO_SEND_ENABLED=false` 是一期安全默认值。
- 不把原始 EML、附件二进制、完整 AI prompt 或真实密钥写入数据库或 Git。

---

## 3. 项目目录结构

项目实际目录以 `repair-mail-agent/` 为准。

```text
repair-mail-agent/
  backend/
    app/
      api/v1/             # REST API 路由
      core/               # 数据库、响应、统一枚举
      models/             # SQLAlchemy ORM
      schemas/            # Pydantic DTO
      services/           # 业务流程层
      repositories/       # 数据访问层
      integrations/       # IMAP/SMTP/OSS/AI 外部集成
      workers/            # APScheduler 或后台任务
      middleware/         # 请求上下文、日志、鉴权等
    alembic/              # 数据库迁移
    tests/                # 后端测试
  frontend/
    src/
      api/                # API client
      layouts/            # 控制台布局
      pages/              # 页面
      routes/             # 路由
      stores/             # Zustand 状态
      types/              # TS 类型
      utils/              # 枚举、状态标签、工具函数
  docker/
    backend.Dockerfile
    frontend.Dockerfile
  nginx/
    default.conf
  docs/
    unified-enums.md
    source-documents.md
    remote-mysql-root-deployment.md
  .github/workflows/
    deploy.yml
  docker-compose.yml
  deploy.sh
  README.md
  AI开发进度与任务跟踪.md
```

后端依赖方向必须保持稳定：

```text
api -> services -> repositories -> models
workers -> services
services -> integrations
services -> observability
models 不依赖 services
repositories 不依赖 api
integrations 不依赖业务 service
```

---

## 4. 技术栈与运行形态

| 层级 | 选型 | 约束 |
| --- | --- | --- |
| 后端 | Python 3.11、FastAPI | 必须使用 Python 3.11+，当前代码使用 `StrEnum` |
| ORM | SQLAlchemy 2.x asyncio、asyncmy | 不允许停留在 SQLAlchemy 1.x |
| 数据库 | MySQL 8.x | `utf8mb4`、`utf8mb4_unicode_ci`、`DATETIME(3)` |
| 迁移 | Alembic | 所有结构变更必须迁移化 |
| 配置 | pydantic-settings、`.env` | 真实配置只在私有环境文件或 GitHub Secrets |
| 任务 | APScheduler | 一期轻量后台任务，不引入消息队列 |
| 文件 | 阿里云 OSS | 原始 EML 与附件只进 OSS |
| 邮件 | IMAP、SMTP | IMAP 收信，SMTP 发信由开关控制 |
| AI | 统一 AI Provider | 规则优先，AI 兜底 |
| 前端 | React、TypeScript、Vite | Node 18 建议作为统一构建环境 |
| UI | Ant Design | 控制台为工作台风格，不做营销页 |
| 部署 | Docker Compose、Nginx、GitHub Actions | 外部入口统一走 Nginx |

---

## 5. 开发规范与底层约束

### 5.1 代码分层

- API 层只做鉴权、参数校验、调用 service、返回 DTO。
- Service 层承载业务流程、状态流转、跨模块编排。
- Repository 层封装数据库访问，禁止 API 层直接写复杂查询。
- Integration 层封装 IMAP、SMTP、OSS、AI 等外部系统。
- Worker 层只调用 service，不复制业务逻辑。

### 5.2 数据库约束

- 一期数据库范围统一为 26 张表，包含日志和审计表，不再使用“23 张业务表”作为验收口径。
- 状态字段统一使用 `VARCHAR`，不使用数据库 `ENUM`。
- `parse_results` 使用 `apply_status` 表达解析结果应用状态。
- `oss_objects.object_key` 统一为 `VARCHAR(640)`。
- `workflow_*` 只管理工单状态机，不承载邮件、附件、回复、任务、通知等所有状态。
- 邮件事实、解析候选、工单当前视图必须分离。
- 状态流转必须写 `ticket_status_logs`；字段修改必须写 `field_audit_logs` 和 `operation_logs`。

### 5.3 安全约束

- 真实密码、邮箱凭据、OSS key、AI key、JWT secret 不得进入 Git。
- `.env`、`.env.*`、`logs/` 必须被 Git 忽略；仅保留 `.env.example`。
- MySQL 不开放公网端口，只允许 Docker 内网、服务器本机和 SSH 隧道访问。
- AI 完整 JSONL 日志不得暴露给前端，前端只展示摘要和定位信息。

### 5.4 状态机约束

标准工单状态：

| 状态 | 中文名 | 说明 |
| --- | --- | --- |
| `new_email` | 邮件已入库 | 邮件和附件已归档，尚未解析 |
| `parsed` | 已解析 | 已产生结构化候选，仍需完整性和 SN 校验 |
| `need_customer_info` | 待客户补充 | 缺少关键字段，需要客户补充 |
| `auto_replied` | 已追问 | 已生成追问草稿或已发送追问 |
| `manual_review` | 人工复核 | 低置信度、异常、冲突或超限 |
| `ready_for_export` | 可导出 | 字段完整，SN 校验通过 |
| `error` | 异常 | 系统处理异常 |
| `closed` | 已关闭 | 工单完成或终止 |

标准流转必须通过 `workflow_transitions` 和 `/api/v1/tickets/{id}/transition`：

| 来源 | 目标 | 事件 | 条件 |
| --- | --- | --- | --- |
| `new_email` | `parsed` | `parse_completed` | 至少产生一个解析结果 |
| `new_email` | `manual_review` | `parse_low_confidence` | 初始解析低置信度 |
| `parsed` | `ready_for_export` | `validation_passed` | 字段完整且 SN 校验通过 |
| `parsed` | `need_customer_info` | `missing_fields_detected` | 关键字段缺失且可追问 |
| `parsed` | `manual_review` | `field_conflict` | 字段冲突、SN 异常或置信度不足 |
| `need_customer_info` | `auto_replied` | `reply_draft_created` | 已生成追问草稿 |
| `auto_replied` | `parsed` | `customer_reply_received` | 客户回复归并原线程 |
| 任意非终态 | `manual_review` | `manual_review_required` | 需要人工介入 |
| `manual_review` | `ready_for_export` | `manual_resolved` | 人工修正且校验通过 |
| `manual_review` | `parsed` | `manual_resolved` | 人工修正后回到解析态 |
| 任意非终态 | `error` | `system_error` | 系统异常 |
| 任意非终态 | `closed` | `manual_close` | 人工关闭 |
| `error` | `parsed` | `manual_resolved` | 异常修复后人工恢复 |
| `ready_for_export` | `closed` | `export_completed` | 导出或内部处理完成 |

禁止绕过状态机直接更新 `repair_tickets.current_status_code`。

### 5.5 AI 与人工协作约束

- 规则解析优先，AI 兜底。
- AI 输出必须是结构化 JSON，并通过后端 schema 校验。
- AI 不得静默覆盖 `manual_locked=1` 字段。
- 低置信度、字段冲突、SN 不确定、解析异常必须转人工。
- 人工修正后的字段优先级高于 AI 和自动解析。

字段合并优先级：

```text
人工锁定字段
> 人工确认字段
> 最新客户回复中的高置信度字段
> 附件结构化字段
> 历史邮件字段
> AI 推测字段
```

---

## 6. 功能开发详细描述

### 6.1 功能模块划分

| 模块 | 类型 | 职责 |
| --- | --- | --- |
| `auth` | 基础设施 | 登录、JWT、角色权限、当前用户 |
| `mail_ingest` | 业务 | IMAP 拉取、去重、前置分类、EML/附件 OSS 上传、邮件入库、任务日志 |
| `mail_threading` | 业务 | 回复链识别、线程归并、人工合并/拆分、线程置信度 |
| `parser` | 业务 | 正文清洗、模板解析、附件解析、字段标准化、解析候选入库 |
| `ai_provider` | 业务 | 邮件分类、字段抽取、追问草稿、回复审查、AI 摘要、JSONL 日志 |
| `sn_registry` | 业务 | SN/资产库、板卡规则导入与校验 |
| `ticket_workflow` | 业务 | 工单生成、状态机、乐观锁、状态日志 |
| `reply` | 业务 | 模板匹配、草稿生成、审核、可选 SMTP 发送、外发邮件归档 |
| `manual_review` | 业务 | 人工任务创建、领取、分配、解决、关闭 |
| `notification` | 基础设施 | 站内通知、SSE、轮询兜底 |
| `observability` | 基础设施 | 操作日志、字段审计、系统事件、后台任务、AI 摘要查询 |
| `system_config` | 基础设施 | 一期只读展示脱敏配置和运行开关 |
| `export` | 后续扩展 | 一期仅以 `ready_for_export` 标记可导出 |

### 6.2 邮件摄取与前置分类

邮件摄取必须先做轻量分类，再决定是否完整归档和深度解析。

```text
1. 创建 job_run_logs，job_type=imap_fetch，状态 running
2. 连接 IMAP，按邮箱、文件夹、UID 拉取候选邮件
3. 仅解析邮件头、主题、发件人、正文摘要、HTML 摘要、附件元数据
4. 计算 message_id_hash，检查重复
5. 执行 L1 邮件类型轻量分类
6. 明确无关邮件按 INGEST_UNRELATED_POLICY 处理
7. 报修相关、客户补充、内部转发疑似报修或 unknown 获取完整 EML 和附件
8. 原始 EML 与附件上传 OSS
9. 写 oss_objects、emails、email_attachments
10. 调用 mail_threading 线程归并
11. 调用 parser 深度解析
12. 调用 SN 校验
13. 创建或更新工单
14. 状态流转、生成追问草稿或创建人工任务
15. 更新 job_run_logs，记录拉取、跳过、归档、解析、异常数量
```

处理分层：

- L0 基础预处理：不调用 AI，只解析 Message-ID、In-Reply-To、References、From/To/Cc、主题、正文摘要、附件元数据和 hash。
- L1 轻量分类：判断 `new_repair`、`customer_reply`、`internal_forward`、`irrelevant`、`unknown`，输出置信度、原因、是否归档、是否深度解析。
- L2 专业字段解析：模板解析、正文解析、附件解析、AI 字段抽取、缺失字段判断、冲突判断、SN 校验、工单创建或更新。

无关邮件策略：

```text
INGEST_UNRELATED_POLICY=ignore / metadata_only / archive
```

一期推荐 `metadata_only`，只保存最小元数据或任务上下文，不把无关邮件正文和附件默认放入业务 OSS。

### 6.3 邮件线程与复杂回复链

必须区分以下对象：

```text
email_threads = 邮件会话容器
emails = 每一封真实邮件
email_attachments = 每封邮件的附件
repair_tickets = 报修业务工单
email_ticket_links = 邮件与工单的关联关系
parse_results.evidence = 字段来源证据
```

严禁把邮件线程直接等同于工单。可能关系包括：

- 一个 thread 对一个 ticket。
- 一封邮件多个 SN，可建一个 ticket 多个 ticket_items，也可人工拆分为多个 tickets。
- 一个 thread 混入多个报修时，需要人工拆分或多 ticket 关联。
- 客户换主题补充旧工单时，多个 threads 可关联同一个 ticket。
- 内部转发多个客户问题时，必须进入人工确认。

线程归并优先级：

1. `In-Reply-To` 或 `References` 精确命中已入库邮件。
2. 客户回复引用系统外发追问的 `smtp_message_id`。
3. 归一化主题相同、发件人域名相同、时间窗口合理。
4. 正文或附件中 SN 与已有工单高度一致。
5. AI 判断上下文一致。
6. 低置信度创建 `thread_uncertain` 人工任务。

自动归并必须记录 `merge_confidence`、`merge_reason`、`merge_strategy`，并在前端可展示。

### 6.4 解析、证据链与重新解析

`parse_results` 保存解析候选，工单表保存当前采纳视图。每个字段候选必须能追溯到邮件正文、附件、表格行、AI 输出或人工修改。

字段证据示例应包含：

- `field`：字段名。
- `value`：候选值。
- `source_type`：最新回复正文、历史正文、附件、AI、人工。
- `email_id`、`attachment_id`、文件名、sheet、row、snippet。
- `confidence`：字段置信度。

重新解析分两种：

- `field_extract`：字段提取错误、模板规则更新、附件解析能力升级、AI 模型升级。
- `classification_and_extract`：邮件类型误判、无关邮件被确认是报修、内部转发误判。

重新解析必须遵守：

- 不得覆盖人工锁定字段。
- 每次重解析生成新的 `parse_results`，不能覆盖旧结果。
- 旧解析结果保留，并通过 `apply_status` 标识状态。
- 重解析原因写 `operation_logs`。
- AI 重解析写新的 `ai_call_logs` 和 JSONL 定位。

### 6.5 附件处理

所有附件必须先关联邮件，再通过邮件关联工单。

```text
emails 1 - N email_attachments
emails N - N repair_tickets，通过 email_ticket_links
```

支持解析：xlsx、xls、txt、pdf、docx。

一期不直接解析：rar、prc、图片 OCR、未知二进制。此类附件仍可按策略入 OSS，附件状态为 `skipped` 或 `failed`，并记录原因。如果正文信息不足且关键信息疑似在不可解析附件中，必须创建人工复核任务。

附件展示、解析和证据记录至少包含 `email_id`、`attachment_id`、`file_name`、`mime_type`、`file_size`、`oss_object_id`、`parse_status`、`used_fields`。

### 6.6 SN 校验与基础资料

SN 校验模块使用 `sn_assets` 和 `board_cards`：

- `sn_assets` 保存 SN、客户、物料、状态和导入来源。
- `board_cards` 保存板卡/物料是否需要寄北京等规则。
- `sn_validation_results` 保存每次校验结果、匹配资产、状态和提示信息。

校验结果状态：`pending`、`pass`、`warning`、`failed`。

SN 异常、客户不匹配、物料不匹配、状态无效或无法确认时，应进入人工复核。

### 6.7 追问、回复审核与外发归档

回复模块负责追问模板匹配、AI 草稿、人工审核、可选 SMTP 发送和外发邮件归档。

模板类型统一为：

- `missing_fields`：缺失字段追问。
- `sn_invalid`：SN 确认。
- `manual_review`：人工审核提醒。

模板匹配规则：

- `need_customer_info` 默认匹配 `missing_fields`。
- 缺失字段含 SN 相关内容时优先匹配 `sn_invalid`。
- 人工复核触发的提醒匹配 `manual_review`。
- 语言按客户邮件语言推断，无法判断默认 `zh-CN`。

`AUTO_SEND_ENABLED=false` 时：

- 允许生成草稿和审核记录。
- 审核通过不等于真实 SMTP 发送。
- 不得伪造 outbound 邮件。
- 接口应返回发送开关状态，让前端展示“已审核，未真实发送”。

`AUTO_SEND_ENABLED=true` 且 SMTP 发送成功时：

```text
生成 reply_records
-> 审核通过
-> SMTP 发送
-> 记录 smtp_message_id
-> 创建 emails，mail_direction=outbound
-> 写 email_ticket_links，link_type=outbound
-> 客户回复时通过 In-Reply-To / References 归并原 thread 和 ticket
```

追问超过 `MAX_FOLLOW_UP=3` 后，转人工复核。追问次数按工单维度计数，每次进入 `auto_replied` 状态 +1；超限时标记 `followup_limit_exceeded` 并创建人工任务和通知。

### 6.8 人工复核任务包

进入 `manual_review` 时，必须创建可操作的 `manual_review_tasks`，不能只改工单状态。

必须创建人工任务的场景：

| 场景 | 任务类型建议 | 说明 |
| --- | --- | --- |
| 解析低置信度 | `low_confidence` 或 `parse_low_confidence` | 无法可靠抽取字段 |
| SN 异常 | `sn_error` 或 `sn_exception` | SN 不存在、无效、客户/物料不匹配 |
| 字段冲突 | `field_conflict` | 新字段与已有字段冲突 |
| 追问超限 | `followup_limit` | 超过 `MAX_FOLLOW_UP` |
| 回复待审核 | `reply_review` | 追问草稿需要人工审核 |
| 线程不确定 | `thread_uncertain` | 无法确定邮件归属 |
| 系统异常 | `system_error` | AI、OSS、IMAP、SMTP、附件解析等异常 |
| 人工主动创建 | `manual` 或 `manual_created` | 用户主动发起复核 |

任务创建必须同时写：

```text
manual_review_tasks
notification_events
operation_logs 或 system_event_logs
```

任务包建议包含：

- 任务类型、优先级、状态、负责人。
- 触发原因、建议动作、任务上下文。
- 工单摘要、触发邮件摘要、缺失字段、冲突字段、SN 校验摘要、解析候选、AI 摘要。
- `dedupe_key` 防止同一工单同一异常重复创建任务。

一期采用“任务池 + 人工领取 + 管理员指派”。管理员可分配、转派、释放和强制关闭任务。

人工任务完成时不等于工单完成，`resolve` 接口必须支持 `next_action`：

| `next_action` | 结果 |
| --- | --- |
| `transition_ready_for_export` | 工单进入可导出 |
| `generate_followup` | 生成追问草稿 |
| `wait_customer_info` | 保持待客户补充 |
| `reparse` | 重新解析 |
| `close_ticket` | 人工关闭工单 |
| `keep_manual_review` | 任务完成但工单仍需其他人工处理 |

### 6.9 前端工作台设计

人工复核页不是普通列表页，而是高频处理工作台。

推荐布局：

```text
左侧：任务队列
中间：邮件证据 / 附件 / 回复链
右侧：系统建议动作 / 工单字段 / 快捷操作
```

人工复核工作台必须支持：

- 领取、释放、转派任务。
- 查看工单、触发邮件、字段证据、附件。
- 应用解析结果、修改字段、修改明细、选择 SN 候选。
- 重新解析、合并线程、拆分线程。
- 生成追问草稿、审核追问草稿。
- 关闭工单、完成任务。

工单详情页必须从“单邮件详情”升级为“工单上下文工作台”，至少包含：

- 工单基础信息。
- SN/板卡明细。
- 邮件会话时间线。
- 附件分组列表。
- 解析候选。
- 字段来源证据。
- SN 校验结果。
- 回复记录。
- 状态日志、字段审计、操作日志。

---

## 7. API 设计摘要

API 前缀统一 `/api/v1`。除登录和健康检查外，业务接口默认需要登录。

统一响应建议：

```json
{
  "success": true,
  "data": {},
  "message": "ok",
  "request_id": "..."
}
```

错误码使用大写蛇形命名，如 `AUTH_INVALID_CREDENTIALS`、`TICKET_VERSION_CONFLICT`、`REPLY_SEND_DISABLED`。

核心接口摘要：

| 模块 | 接口 |
| --- | --- |
| 认证 | `POST /auth/login`、`GET /auth/me` |
| 看板 | `GET /dashboard/summary` |
| 邮件 | `GET /emails`、`GET /emails/{id}`、`POST /emails/{id}/reparse` |
| 邮件线程 | `POST /email-threads/{id}/merge`、`POST /email-threads/{id}/split` |
| 工单 | `GET /tickets`、`GET /tickets/{id}`、`PATCH /tickets/{id}/fields`、`POST /tickets/{id}/transition` |
| 解析结果 | `GET /tickets/{id}/parse-results`、`POST /parse-results/{id}/apply` |
| 人工复核 | `GET /manual-review/tasks`、`GET /manual-review/tasks/{id}`、`POST /manual-review/tasks/{id}/claim`、`POST /manual-review/tasks/{id}/assign`、`POST /manual-review/tasks/{id}/resolve`、`POST /manual-review/tasks/{id}/reparse` |
| 回复 | `POST /replies/{ticket_id}/draft`、`PATCH /replies/{reply_id}`、`POST /replies/{reply_id}/approve-send`、`POST /replies/{reply_id}/reject` |
| 基础资料 | `POST /master-data/sn-assets/import`、`GET /master-data/sn-assets`、`POST /master-data/board-cards/import` |
| AI 与通知 | `GET /ai-logs`、`GET /notifications`、`GET /notifications/stream` |
| 工单上下文补充 | `GET /tickets/{id}/email-timeline`、`GET /tickets/{id}/attachments`、`GET /tickets/{id}/field-evidence` |

列表接口必须分页，默认 `page=1&page_size=20`，最大 `page_size=100`。

---

## 8. 数据库表与关键字段摘要

本文只列关键字段和设计意图。完整字段、索引、外键以 `邮件报修自动化系统数据库表字段设计方案_一期最终版.md` 和 ORM/Alembic 为准。

### 8.1 用户权限

| 表 | 作用 | 关键字段 |
| --- | --- | --- |
| `users` | 控制台用户 | `username`、`email`、`password_hash`、`real_name`、`status`、`last_login_at` |
| `roles` | 角色定义 | `role_code`、`role_name`、`description` |
| `user_roles` | 用户角色关系 | `user_id`、`role_id` |

标准角色：`admin`、`supervisor`、`operator`。

### 8.2 OSS 与邮件归档

| 表 | 作用 | 关键字段 |
| --- | --- | --- |
| `oss_objects` | OSS 对象元数据 | `bucket`、`object_key`、`file_name`、`content_type`、`file_size`、`sha256_hash`、`source_type`、`upload_status` |
| `email_threads` | 邮件会话容器 | `thread_key`、`normalized_subject`、`root_message_id`、`latest_email_id`、`ticket_id`、`email_count` |
| `emails` | 每一封邮件事实 | `message_id`、`message_id_hash`、`mail_direction`、`from_address`、`to_addresses`、`subject`、`received_at`、`intent_type`、`parse_status`、`raw_eml_oss_object_id` |
| `email_attachments` | 邮件附件 | `email_id`、`oss_object_id`、`file_name`、`mime_type`、`file_size`、`file_hash`、`parse_status` |
| `email_ticket_links` | 邮件工单关联 | `email_id`、`ticket_id`、`link_type`、`linked_by_user_id` |

关键裁定：

- `message_id_hash` 是邮件唯一去重主依据。
- 原始 EML 与附件只进 OSS，数据库只存对象引用和元数据。
- 附件先关联邮件，再通过邮件关联工单。

### 8.3 工单与状态机

| 表 | 作用 | 关键字段 |
| --- | --- | --- |
| `repair_tickets` | 工单主表 | `ticket_no`、`current_status_code`、`source_email_id`、`thread_id`、`customer_code`、`customer_name`、`contact_name`、`contact_email`、`followup_count`、`assigned_user_id`、`version` |
| `repair_ticket_items` | 工单明细 | `ticket_id`、`line_no`、`sn`、`material_code`、`fault_description`、`validation_status`、`manual_locked` |
| `workflow_statuses` | 状态定义 | `status_code`、`status_name`、`status_category`、`is_terminal` |
| `workflow_transitions` | 状态流转规则 | `from_status_code`、`to_status_code`、`trigger_event`、`condition_desc`、`require_manual` |
| `ticket_status_logs` | 状态日志 | `ticket_id`、`from_status_code`、`to_status_code`、`trigger_event`、`operator_user_id`、`reason` |
| `field_audit_logs` | 字段审计 | `ticket_id`、`ticket_item_id`、`field_name`、`old_value`、`new_value`、`operator_user_id`、`parse_result_id` |

关键裁定：

- 任何状态变化都必须走状态机。
- 字段修改必须保留 old/new 和操作者。
- `version` 用于乐观锁，防止多人并发覆盖。

### 8.4 解析与 SN 校验

| 表 | 作用 | 关键字段 |
| --- | --- | --- |
| `parse_results` | 结构化解析候选 | `email_id`、`source_attachment_id`、`ticket_id`、`parser_type`、`parser_version`、`confidence`、`parsed_json`、`evidence_json`、`apply_status`、`applied_by_user_id` |
| `sn_validation_results` | SN 校验结果 | `ticket_id`、`ticket_item_id`、`sn`、`result_status`、`result_message`、`matched_sn_asset_id` |
| `sn_assets` | SN/资产库 | `sn`、`customer_code`、`customer_name`、`material_code`、`material_name`、`asset_status`、`source_file_hash` |
| `board_cards` | 板卡/物料规则 | `material_code`、`material_name`、`need_ship_to_beijing`、`status` |

关键裁定：

- `parse_results` 是候选和证据链，不是工单当前视图。
- SN 异常或不确定时转人工。
- 基础资料导入必须写任务日志和异常行摘要。

### 8.5 回复、人工复核与通知

| 表 | 作用 | 关键字段 |
| --- | --- | --- |
| `reply_templates` | 回复模板 | `template_code`、`version`、`template_type`、`language`、`subject_template`、`body_template`、`enabled` |
| `reply_records` | 回复记录 | `ticket_id`、`related_email_id`、`outgoing_email_id`、`template_id`、`draft_body`、`final_body`、`review_status`、`send_status`、`followup_round`、`smtp_message_id` |
| `manual_review_tasks` | 人工任务 | `ticket_id`、`email_id`、`task_type`、`priority`、`status`、`assigned_user_id`、`claimed_by_user_id`、`resolved_by_user_id`、`context_json` |
| `notification_events` | 站内通知 | `event_type`、`recipient_user_id`、`recipient_role_code`、`target_type`、`target_id`、`delivery_status`、`payload_json` |

关键裁定：

- 回复审核和真实发送是两件事。
- 进入人工状态必须创建任务包。
- 通知模块提供 SSE 和轮询兜底。

### 8.6 日志与审计

| 表 | 作用 | 关键字段 |
| --- | --- | --- |
| `ai_call_logs` | AI 调用摘要索引 | `trace_id`、`email_id`、`ticket_id`、`call_type`、`provider`、`model`、`status`、`jsonl_path`、`jsonl_line_no` |
| `operation_logs` | 用户操作日志 | `user_id`、`operation_type`、`target_type`、`target_id`、`detail_json` |
| `system_event_logs` | 系统事件日志 | `event_type`、`severity`、`message`、`correlation_id`、`email_id`、`ticket_id`、`job_run_id` |
| `job_run_logs` | 后台任务日志 | `job_name`、`job_type`、`status`、`started_at`、`ended_at`、`summary_json`、`error_message` |

关键裁定：

- 后台任务开始和结束必须写 `job_run_logs`。
- IMAP/SMTP/OSS/AI/DB 异常必须写 `system_event_logs`。
- AI 完整日志写 JSONL，数据库仅存摘要和定位。

---

## 9. 开发、部署与测试流程

### 9.1 本地开发前置条件

- Python 3.11+。
- Node 18+。
- Docker 与 Docker Compose。
- Git 能访问 GitHub 私有仓库。
- 如连接远程开发库，需要 SSH 隧道：

```powershell
ssh -N -L 13307:127.0.0.1:3307 <server-alias>
```

本地 `.env` 示例：

```text
DATABASE_URL=mysql+asyncmy://root:<ROOT_PASSWORD>@127.0.0.1:13307/repair_system_dev
```

远程 Docker Compose 内部：

```text
DATABASE_URL=mysql+asyncmy://root:<ROOT_PASSWORD>@mysql:3306/repair_system_dev
```

真实密码必须 URL 编码并只写入私有 `.env`。

### 9.2 后端开发命令

```bash
cd repair-mail-agent/backend
python -m venv .venv
pip install -r requirements.txt
python -m compileall app
pytest
uvicorn app.main:app --reload
```

数据库迁移和初始化：

```bash
cd repair-mail-agent/backend
alembic upgrade head
python -m app.seed
python -m app.db_smoke
```

模型变更流程：

```bash
alembic revision --autogenerate -m "describe schema change"
alembic upgrade head
python -m app.seed
python -m app.db_smoke
pytest
```

### 9.3 前端开发命令

```bash
cd repair-mail-agent/frontend
npm install
npm run typecheck
npm run build
npm run dev
```

前端开发必须同步 `frontend/src/types`、API client、页面状态和统一枚举标签。

### 9.4 Docker 本地或远程启动

```bash
cd repair-mail-agent
cp .env.example .env
# 立即替换 MYSQL_ROOT_PASSWORD、DATABASE_URL、JWT_SECRET 等占位值
docker compose up -d mysql
docker compose up -d --build
docker compose logs -f backend-api
docker compose logs -f nginx
```

容器接入顺序：

1. `mysql`
2. 后端迁移/初始化任务
3. `backend-api`
4. `frontend`
5. `nginx`
6. GitHub Actions 自动部署

### 9.5 标准部署脚本要求

`deploy.sh` 必须至少包含：

1. 确认项目目录和 `.env` 存在。
2. `git pull --ff-only origin main`。
3. 部署前备份 MySQL。
4. `docker compose up -d mysql` 并等待健康。
5. 执行 Alembic 迁移，失败必须退出。
6. 执行 seed。
7. `docker compose up -d --build`。
8. 检查 `/health` 返回成功。
9. 输出 `docker compose ps` 和日志定位。

迁移失败、构建失败、健康检查失败时必须 `exit 1`，不得让 CI/CD 显示成功。

### 9.6 GitHub Actions 要求

GitHub Secrets：

- `SERVER_HOST`
- `SERVER_USER`
- `SERVER_PORT`
- `SERVER_SSH_KEY`
- `SERVER_PROJECT_DIR`

CI/CD 最低要求：

- main 分支 push 触发。
- SSH 到服务器执行 `deploy.sh`。
- 后续应增加后端 pytest、前端 typecheck/build、Docker 构建检查。
- 不允许在服务器手工改代码绕过 GitHub。

### 9.7 测试验收

阶段性最低验收：

- 后端：`python -m compileall app`、`pytest`、`alembic upgrade head`、`python -m app.seed`、`python -m app.db_smoke`。
- 前端：`npm run typecheck`、`npm run build`。
- Docker：`docker compose up -d --build`、`/health` 可访问。
- 数据库：26 张业务/审计表 + `alembic_version`；基础种子数据存在。
- Git：本地分支跟踪 `origin/main`，工作区提交前不包含 `.env`、日志、真实密钥。
- 业务模块：只有 DTO、权限、业务逻辑、测试、前端联调和文档均完成后，才能标记为完成。

---

## 10. 日志与监控

一期排障以 Docker logs、Nginx logs 和数据库审计表为基础。

必须持久化：

- backend-api：`./logs:/app/logs`
- nginx：`./logs/nginx:/var/log/nginx`

AI JSONL 路径：

```text
logs/ai/{yyyy}/{mm}/{dd}/ai-{yyyyMMdd}.jsonl
```

日志目录不得进入 Git。

数据库日志职责：

- `operation_logs`：用户动作。
- `field_audit_logs`：字段 old/new。
- `system_event_logs`：系统异常和事件。
- `job_run_logs`：后台任务开始、结束、统计、异常。
- `ticket_status_logs`：工单状态变化。
- `ai_call_logs`：AI 调用摘要索引。

---

## 11. 当前开发基线与待办

### 11.1 当前基线

当前真实状态以 `repair-mail-agent/AI开发进度与任务跟踪.md` 为准：

- 阶段 1 项目底座已基本完成。
- 一期 26 张表 ORM、迁移、基础种子数据已落地。
- React + Ant Design 控制台骨架已落地。
- Docker Compose、Nginx、GitHub Actions 和 deploy 脚本已有初版。
- GitHub 私有仓库、远程 deploy key、远程拉取链路已有历史记录。

### 11.2 当前主要缺口

- 认证、邮件、工单、人工复核、回复、基础资料、AI 日志、通知等业务 API 仍主要是占位。
- `deploy.sh` 仍需补齐备份、迁移、seed、健康检查和失败退出。
- `docker-compose.yml` 仍需补齐 backend 和 nginx 日志卷。
- `seed.py` 状态中文名、`status_category`、部分流转和统一枚举仍需收口。
- 前端页面仍以骨架和占位页为主，需要联调真实接口。
- 真实 IMAP/SMTP/OSS/AI 配置尚未完成联调。

### 11.3 下一步建议顺序

1. 修正 `seed.py`，对齐统一枚举、状态中文名、状态类别和缺失流转。
2. 重构 `deploy.sh`，补齐备份、迁移、seed、健康检查。
3. 修改 `docker-compose.yml`，增加日志持久化挂载。
4. 补齐 CI/CD 测试门禁。
5. 实现 auth 基础能力。
6. 实现邮件摄取、前置分类、OSS 归档和线程归并。
7. 实现 parser、SN 校验、工单生成和状态机。
8. 实现人工任务包、自动回复审核和通知。
9. 实现工单上下文工作台、邮件时间线、附件分组和字段证据。

---

## 12. 原文档保留说明

以下文档不删除，但后续不应并列作为开发入口。

| 文档 | 当前定位 | 何时查阅 |
| --- | --- | --- |
| `邮件报修自动化系统统一开发总结文档.md` | 统一入口和当前主文档 | 每次开发前优先读 |
| `邮件报修自动化系统PRD技术方案.md` | 原始业务目标、一期范围、业务验收依据 | 业务目标争议、需求边界争议 |
| `邮件报修自动化系统详细开发设计文档.md` | 原详细设计，已被增强版覆盖大部分内容 | 仅用于追溯旧版本设计 |
| `邮件报修自动化系统详细开发设计文档_增强版.md` | 增强业务细节来源 | 需要查看前置分类、人工任务包、复杂回复链的原始长描述 |
| `邮件报修自动化系统数据库表字段设计方案_一期最终版.md` | 全字段数据库字典 | 表字段、索引、外键、字段类型细节 |
| `文档一致性审查报告.md` | 多文档冲突清单与裁定依据 | 发现口径冲突或需要查看问题编号 |
| `Codex_Docker_CICD_标准研发与部署流程.md` | 通用研发部署标准历史文档 | 需要查看通用流程原文 |
| `远程服务器Docker容器化开发部署执行顺序说明.md` | 远程部署步骤历史文档 | 需要查看远程 MySQL、SSH 隧道、部署阶段细节 |
| `codex-git-01.md` | GitHub、远程 MySQL 和 deploy key 历史记录 | 需要追溯 2026-07-02/03 的验证过程 |
| `repair-mail-agent/README.md` | 项目运行说明 | 实际启动、环境变量、测试命令 |
| `repair-mail-agent/AI开发进度与任务跟踪.md` | 当前真实进度 | 判断模块是否完成、是否阻塞 |
| `repair-mail-agent/docs/unified-enums.md` | 枚举清单 | 新增或修改状态、角色、任务、通知、回复状态 |
| `repair-mail-agent/docs/remote-mysql-root-deployment.md` | 当前远程 MySQL 开发基线 | 数据库连接、SSH 隧道、远程 MySQL 验证 |

如果后续继续整理文档，建议将旧顶层文档移动到 `docs/archive/` 或在文件名增加 `历史参考` 标记；在未确认团队引用关系前，不建议直接删除。

---

## 13. 开发前检查清单

每次开发前确认：

- 已阅读本文、项目 README、AI 开发进度文档。
- 当前工作区没有误提交 `.env`、日志、真实密钥。
- 新增枚举同步到 `docs/unified-enums.md`、后端 `app/core/enums.py`、前端 `src/utils/enums.ts` 和 seed。
- 数据库结构变化有 Alembic 迁移。
- 状态变更走状态机，字段变更写审计。
- AI 结果不覆盖人工锁定字段。
- 自动回复真实发送开关默认关闭。
- 涉及部署时先确认备份和健康检查。
- 标记模块完成前，必须完成 DTO、权限、业务逻辑、测试、前端联调和文档同步。
