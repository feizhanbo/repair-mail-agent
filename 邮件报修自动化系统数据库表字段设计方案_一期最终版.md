# 邮件报修自动化系统数据库表字段设计方案（一期最终版）

生成日期：2026-06-30

## 当前实现基线（2026-07-05）

本文件仍作为一期数据库设计主参考。当前代码和迁移已对齐以下事实：

- 一期业务表为 26 张，另有 Alembic 管理表 `alembic_version`。
- 最新迁移为 `9d2b7c4f1a30_add_parse_result_apply_status.py`。
- `parse_results.apply_status`、`applied_by_user_id`、`applied_at` 已落地。
- 预置角色统一为 `admin`、`supervisor`、`operator`；旧版 `viewer` 不再作为当前开发方向。
- 远程新库 `repair_system_codex_dev_test` 已迁移到 `9d2b7c4f1a30`，保留临时管理员 `codex_admin_validation`；验证用户已通过前端删除。
- 真实数据库口令不写入本文档。

## 1. 版本结论

本版基于 PRD 技术方案和评审反馈整理，作为一期数据库设计准稿。


本版一期共 **26 张表**。

## 2. 表清单

| 序号 | 分组 | 表名 | 说明 |
| --- | --- | --- | --- |
| 1 | 用户权限 | `users` | 控制台用户 |
| 2 | 用户权限 | `roles` | 角色 |
| 3 | 用户权限 | `user_roles` | 用户角色关系 |
| 4 | OSS 文件 | `oss_objects` | OSS 对象元数据 |
| 5 | 邮件归档 | `email_threads` | 邮件线程 |
| 6 | 邮件归档 | `emails` | 邮件存档 |
| 7 | 邮件归档 | `email_attachments` | 邮件附件 |
| 8 | 邮件归档 | `email_ticket_links` | 邮件工单关联 |
| 9 | 工单 | `repair_tickets` | 工单主表 |
| 10 | 工单 | `repair_ticket_items` | 工单明细 |
| 11 | 状态机 | `workflow_statuses` | 状态定义 |
| 12 | 状态机 | `workflow_transitions` | 状态流转规则 |
| 13 | 状态机 | `ticket_status_logs` | 工单状态日志 |
| 14 | 状态机 | `field_audit_logs` | 字段修改审计 |
| 15 | 解析校验 | `parse_results` | 解析结果 |
| 16 | 解析校验 | `sn_validation_results` | SN 校验结果 |
| 17 | 基础资料 | `sn_assets` | SN/资产校验库 |
| 18 | 基础资料 | `board_cards` | 板卡/物料寄北京规则 |
| 19 | 回复 | `reply_templates` | 回复模板 |
| 20 | 回复 | `reply_records` | 回复记录 |
| 21 | 人工复核 | `manual_review_tasks` | 人工复核任务 |
| 22 | 人工复核 | `notification_events` | 站内通知事件 |
| 23 | AI 日志 | `ai_call_logs` | AI 调用摘要索引 |
| 24 | 日志 | `operation_logs` | 用户操作日志 |
| 25 | 日志 | `system_event_logs` | 系统事件日志 |
| 26 | 日志 | `job_run_logs` | 后台任务运行日志 |

注意：若严格只统计业务表，不含日志表则为 23 张；若统计所有一期数据库表，则为 **26 张**。

## 3. 核心状态与类型

### 3.1 工单状态

| 状态码 | 中文名 | 说明 |
| --- | --- | --- |
| `new_email` | 邮件已入库 | 邮件和附件已归档，尚未解析 |
| `parsed` | 已解析 | 已产生结构化字段，但仍需完整性和 SN 校验 |
| `need_customer_info` | 等待客户补充 | 缺少关键字段 |
| `auto_replied` | 已追问 | 已生成或发送追问邮件 |
| `manual_review` | 人工复核 | 低置信度、SN 异常、追问超限或系统异常 |
| `ready_for_export` | 可导出 | 字段完整，SN 校验通过 |
| `error` | 异常 | 系统处理异常 |
| `closed` | 已关闭 | 工单完成或终止 |

### 3.2 邮件意图

| 值 | 说明 |
| --- | --- |
| `new_repair` | 新报修 |
| `customer_reply` | 客户补充信息 |
| `internal_forward` | 内部转发 |
| `irrelevant` | 无关邮件 |
| `unknown` | 未知 |

### 3.3 人工任务类型

| 值 | 说明 |
| --- | --- |
| `low_confidence` | 低置信度 |
| `sn_error` | SN 异常 |
| `followup_limit` | 追问超限 |
| `system_error` | 系统异常 |
| `field_conflict` | 字段冲突 |
| `manual` | 人工创建 |

## 4. 表字段设计

### 4.1 `users` 控制台用户表

用途：控制台登录、人工复核、回复审核、操作审计。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 用户 ID |
| `username` | VARCHAR(64) | 是 |  | 登录账号，唯一 |
| `password_hash` | VARCHAR(255) | 是 |  | 密码哈希，不保存明文 |
| `real_name` | VARCHAR(64) | 是 |  | 真实姓名 |
| `email` | VARCHAR(255) | 否 |  | 工作邮箱 |
| `phone` | VARCHAR(50) | 否 |  | 联系电话 |
| `department` | VARCHAR(100) | 否 |  | 所属部门 |
| `status` | VARCHAR(20) | 是 | `active` | `active` / `disabled` |
| `last_login_at` | DATETIME(3) | 否 |  | 最后登录时间 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |
| `updated_at` | DATETIME(3) | 是 | 当前时间 | 更新时间 |

索引：

- `uk_users_username(username)`
- `uk_users_email(email)`
- `idx_users_status(status)`

### 4.2 `roles` 角色表

用途：控制台角色定义。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 角色 ID |
| `role_code` | VARCHAR(50) | 是 |  | 角色编码，唯一 |
| `role_name` | VARCHAR(100) | 是 |  | 角色名称 |
| `description` | VARCHAR(500) | 否 |  | 角色说明 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |
| `updated_at` | DATETIME(3) | 是 | 当前时间 | 更新时间 |

预置角色：

| `role_code` | `role_name` | 说明 |
| --- | --- | --- |
| `admin` | 系统管理员 | 用户管理、角色分配、系统配置、基础资料维护、权限配置、全部数据查看和全部业务兜底操作 |
| `supervisor` | 主管 | 查看全部业务数据，任务分配、转派、释放，查看工单、通知和日志摘要，审核回复，处理异常任务 |
| `operator` | 一般操作员 | 处理本人可见或已领取任务，修正字段、SN 校验、采纳解析、生成追问和提交回复草稿 |

索引：

- `uk_roles_code(role_code)`

### 4.3 `user_roles` 用户角色关联表

用途：用户与角色多对多关系。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 主键 |
| `user_id` | BIGINT UNSIGNED | 是 |  | 用户 ID |
| `role_id` | BIGINT UNSIGNED | 是 |  | 角色 ID |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |

索引：

- `uk_user_roles_user_role(user_id, role_id)`
- `idx_user_roles_user(user_id)`
- `idx_user_roles_role(role_id)`

### 4.4 `oss_objects` OSS 对象元数据表

用途：统一保存原始 EML、邮件附件、手工上传文件等 OSS 对象元数据。数据库不保存文件二进制。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | OSS 对象 ID |
| `bucket` | VARCHAR(128) | 是 |  | OSS bucket |
| `endpoint` | VARCHAR(255) | 否 |  | OSS endpoint |
| `object_key` | VARCHAR(640) | 是 |  | OSS object key |
| `object_version` | VARCHAR(128) | 否 |  | OSS version id，未启用版本控制时为空 |
| `original_file_name` | VARCHAR(255) | 否 |  | 原始文件名 |
| `safe_file_name` | VARCHAR(255) | 否 |  | 清洗后的安全文件名 |
| `content_type` | VARCHAR(255) | 否 |  | MIME 类型 |
| `file_size` | BIGINT UNSIGNED | 否 |  | 文件大小，字节 |
| `sha256_hash` | CHAR(64) | 否 |  | 文件 SHA256 |
| `etag` | VARCHAR(128) | 否 |  | OSS ETag |
| `storage_class` | VARCHAR(30) | 否 | `Standard` | OSS 存储类型 |
| `source_type` | VARCHAR(50) | 是 |  | `raw_eml` / `email_attachment` / `manual_upload` |
| `upload_status` | VARCHAR(30) | 是 | `pending` | `pending` / `uploaded` / `failed` |
| `error_message` | TEXT | 否 |  | 上传失败原因 |
| `created_by_user_id` | BIGINT UNSIGNED | 否 |  | 人工上传人 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |

索引：

- `uk_oss_objects_bucket_key(bucket, object_key)`
- `idx_oss_objects_hash(sha256_hash)`
- `idx_oss_objects_source(source_type, created_at)`
- `idx_oss_objects_status(upload_status)`

对象 key 建议：

```text
repair-mail/{env}/{yyyy}/{mm}/{thread_id}/{email_id}/{sha256}-{safe_filename}
```

### 4.5 `email_threads` 邮件线程表

用途：归并同一报修问题的多封邮件。每封邮件仍单独保存在 `emails`，本表只保存线程级元数据。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 邮件线程 ID |
| `thread_key` | VARCHAR(128) | 是 |  | 线程归并键，唯一 |
| `normalized_subject` | VARCHAR(500) | 否 |  | 归一化主题 |
| `root_message_id` | VARCHAR(500) | 否 |  | 根邮件 Message-ID |
| `latest_email_id` | BIGINT UNSIGNED | 否 |  | 最新邮件 ID |
| `ticket_id` | BIGINT UNSIGNED | 否 |  | 关联主工单 ID |
| `email_count` | INT | 是 | 0 | 线程内邮件数量 |
| `merge_confidence` | DECIMAL(5,4) | 否 |  | 自动归并置信度 |
| `merge_reason` | VARCHAR(500) | 否 |  | 归并依据，如 References 命中、主题归一化命中 |
| `manual_locked` | TINYINT(1) | 是 | 0 | 人工锁定后不允许自动拆分或重归并 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |
| `updated_at` | DATETIME(3) | 是 | 当前时间 | 更新时间 |

索引：

- `uk_email_threads_key(thread_key)`
- `idx_email_threads_subject(normalized_subject)`
- `idx_email_threads_ticket(ticket_id)`
- `idx_email_threads_latest(latest_email_id)`

### 4.6 `emails` 邮件存档表

用途：每封真实邮件单独保存一行。回复链邮件不拼接正文，靠 `thread_id` 归入同一线程。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 邮件 ID |
| `fetch_job_run_id` | BIGINT UNSIGNED | 否 |  | 对应 `job_run_logs.id`，记录哪次任务拉取 |
| `thread_id` | BIGINT UNSIGNED | 否 |  | 邮件线程 ID |
| `raw_eml_oss_object_id` | BIGINT UNSIGNED | 否 |  | 原始 EML 文件 OSS 对象 ID |
| `mail_direction` | VARCHAR(20) | 是 | `inbound` | `inbound` / `outbound` |
| `mailbox_account` | VARCHAR(255) | 是 |  | 收发邮箱账号快照 |
| `folder_name` | VARCHAR(255) | 否 |  | 邮箱文件夹 |
| `imap_uid` | VARCHAR(100) | 否 |  | IMAP UID |
| `message_id` | VARCHAR(500) | 是 |  | 邮件头原始 Message-ID |
| `message_id_hash` | CHAR(64) | 是 |  | Message-ID 的 SHA256，用于唯一去重 |
| `in_reply_to` | VARCHAR(500) | 否 |  | 邮件头 In-Reply-To |
| `references_header` | TEXT | 否 |  | 邮件头 References |
| `raw_headers` | JSON | 否 |  | 完整邮件头 JSON |
| `from_address` | VARCHAR(500) | 是 |  | 发件人 |
| `from_domain` | VARCHAR(255) | 否 |  | 发件人域名 |
| `to_addresses` | TEXT | 否 |  | 收件人 |
| `cc_addresses` | TEXT | 否 |  | 抄送人 |
| `subject` | VARCHAR(500) | 否 |  | 原始主题 |
| `normalized_subject` | VARCHAR(500) | 否 |  | 去除 Re/Fwd/回复/转发后的归一化主题 |
| `sent_at` | DATETIME(3) | 否 |  | 邮件发送时间 |
| `received_at` | DATETIME(3) | 否 |  | 邮件接收时间 |
| `text_body` | MEDIUMTEXT | 否 |  | 纯文本正文 |
| `html_body` | MEDIUMTEXT | 否 |  | HTML 正文 |
| `clean_body` | MEDIUMTEXT | 否 |  | 清洗后的可读正文 |
| `latest_reply_segment` | MEDIUMTEXT | 否 |  | 从回复链中切出的最新回复段 |
| `parse_status` | VARCHAR(30) | 是 | `pending` | `pending` / `parsed` / `failed` / `ignored` |
| `intent_type` | VARCHAR(50) | 否 |  | `new_repair` / `customer_reply` / `internal_forward` / `irrelevant` / `unknown` |
| `duplicate_of_email_id` | BIGINT UNSIGNED | 否 |  | 非关键兼容字段；一期以 `message_id_hash` 唯一约束作为去重主依据 |
| `error_message` | TEXT | 否 |  | 归档或解析错误 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |
| `updated_at` | DATETIME(3) | 是 | 当前时间 | 更新时间 |

索引：

- `uk_emails_message_id_hash(message_id_hash)`
- `idx_emails_thread(thread_id)`
- `idx_emails_received_at(received_at)`
- `idx_emails_fetch_job(fetch_job_run_id)`
- `idx_emails_direction_status_time(mail_direction, parse_status, received_at)`
- `idx_emails_intent(intent_type)`
- `idx_emails_from_domain(from_domain)`

### 4.7 `email_attachments` 邮件附件表

用途：保存附件元数据、附件解析结果和 OSS 对象关系。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 附件 ID |
| `email_id` | BIGINT UNSIGNED | 是 |  | 所属邮件 ID |
| `oss_object_id` | BIGINT UNSIGNED | 否 |  | 附件文件 OSS 对象 ID |
| `file_name` | VARCHAR(255) | 是 |  | 原始文件名 |
| `content_type` | VARCHAR(255) | 否 |  | MIME 类型 |
| `file_size` | BIGINT UNSIGNED | 否 |  | 文件大小 |
| `file_hash` | CHAR(64) | 否 |  | 附件 SHA256 |
| `is_inline` | TINYINT(1) | 是 | 0 | 是否内嵌附件 |
| `content_id` | VARCHAR(255) | 否 |  | 内嵌附件 Content-ID |
| `parse_status` | VARCHAR(30) | 是 | `pending` | `pending` / `parsed` / `failed` / `skipped` |
| `extracted_text` | MEDIUMTEXT | 否 |  | 附件提取文本 |
| `extracted_json` | JSON | 否 |  | Excel 等附件提取出的结构化 JSON |
| `parse_error` | TEXT | 否 |  | 附件解析错误 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |

索引：

- `idx_email_attachments_email(email_id)`
- `idx_email_attachments_oss(oss_object_id)`
- `idx_email_attachments_hash(file_hash)`
- `idx_email_attachments_content_id(content_id)`
- `idx_email_attachments_parse_status(parse_status)`

### 4.8 `email_ticket_links` 邮件工单关联表

用途：精确记录哪些邮件参与了某个工单。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 关联 ID |
| `email_id` | BIGINT UNSIGNED | 是 |  | 邮件 ID |
| `ticket_id` | BIGINT UNSIGNED | 是 |  | 工单 ID |
| `link_type` | VARCHAR(30) | 是 |  | `source` / `reply` / `forward` / `outbound` / `manual` |
| `link_reason` | VARCHAR(500) | 否 |  | 关联原因 |
| `linked_by_user_id` | BIGINT UNSIGNED | 否 |  | 人工关联人 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |

索引：

- `uk_email_ticket_links(email_id, ticket_id, link_type)`
- `idx_email_ticket_links_email(email_id)`
- `idx_email_ticket_links_ticket(ticket_id)`
- `idx_email_ticket_links_type(link_type)`

### 4.9 `repair_tickets` 工单主表

用途：保存当前被采纳后的工单主信息。客户信息从邮件解析和 SN 校验中获得。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 工单 ID |
| `ticket_no` | VARCHAR(50) | 是 |  | 工单编号，唯一 |
| `current_status_code` | VARCHAR(50) | 是 | `new_email` | 当前状态 |
| `source_email_id` | BIGINT UNSIGNED | 否 |  | 首封来源邮件 ID |
| `thread_id` | BIGINT UNSIGNED | 否 |  | 邮件线程 ID |
| `customer_code` | VARCHAR(50) | 否 |  | 客户编号，来自邮件或 SN 库反查 |
| `customer_name` | VARCHAR(255) | 否 |  | 客户名称，来自邮件或 SN 库反查 |
| `contact_person` | VARCHAR(100) | 否 |  | 联系人 |
| `contact_phone` | VARCHAR(100) | 否 |  | 联系电话 |
| `contact_email` | VARCHAR(255) | 否 |  | 联系邮箱 |
| `request_date` | DATE | 否 |  | 报修日期 |
| `mailing_address` | VARCHAR(500) | 否 |  | 返回地址 |
| `problem_description` | TEXT | 否 |  | 问题描述汇总 |
| `accessories` | VARCHAR(500) | 否 |  | 附属物品 |
| `missing_fields` | JSON | 否 |  | 当前缺失字段 |
| `conflict_fields` | JSON | 否 |  | 字段冲突 |
| `followup_count` | INT | 是 | 0 | 已追问次数 |
| `max_followup_count` | INT | 是 | 3 | 最大追问次数 |
| `confidence_score` | DECIMAL(5,4) | 否 |  | 当前解析整体置信度 |
| `assigned_user_id` | BIGINT UNSIGNED | 否 |  | 当前处理人 |
| `manual_locked` | TINYINT(1) | 是 | 0 | 人工锁定，避免自动覆盖 |
| `version` | INT | 是 | 1 | 乐观锁版本号 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |
| `updated_at` | DATETIME(3) | 是 | 当前时间 | 更新时间 |

索引：

- `uk_repair_tickets_no(ticket_no)`
- `idx_repair_tickets_status_updated(current_status_code, updated_at)`
- `idx_repair_tickets_customer_code(customer_code)`
- `idx_repair_tickets_customer_name(customer_name)`
- `idx_repair_tickets_thread(thread_id)`
- `idx_repair_tickets_assignee_status(assigned_user_id, current_status_code)`

约束建议：

- `followup_count >= 0`
- `max_followup_count >= followup_count`
- `confidence_score IS NULL OR confidence_score BETWEEN 0 AND 1`

### 4.10 `repair_ticket_items` 工单明细表

用途：保存工单中的板卡/物料/SN 明细。一封邮件可包含多块板卡或多个备件。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 明细 ID |
| `ticket_id` | BIGINT UNSIGNED | 是 |  | 工单 ID |
| `line_no` | INT | 是 |  | 明细行号 |
| `material_code` | VARCHAR(100) | 否 |  | 物料代码/板卡唯一编号 |
| `material_name` | VARCHAR(255) | 否 |  | 物料名称/板卡名称 |
| `sn` | VARCHAR(100) | 否 |  | 编号/SN |
| `sn_asset_id` | BIGINT UNSIGNED | 否 |  | 匹配到的 SN 资产 ID |
| `quantity` | INT | 是 | 1 | 数量 |
| `failure_description` | TEXT | 否 |  | 故障描述 |
| `failure_information` | TEXT | 否 |  | 故障信息 |
| `data_info` | TEXT | 否 |  | 数据、附件、测试结果说明 |
| `remarks` | TEXT | 否 |  | 备注 |
| `accessories` | VARCHAR(500) | 否 |  | 本行附属物品 |
| `validation_status` | VARCHAR(30) | 是 | `pending` | `pending` / `pass` / `warning` / `failed` |
| `validation_message` | TEXT | 否 |  | 校验说明 |
| `manual_locked` | TINYINT(1) | 是 | 0 | 人工锁定 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |
| `updated_at` | DATETIME(3) | 是 | 当前时间 | 更新时间 |

索引：

- `uk_ticket_items_line(ticket_id, line_no)`
- `idx_ticket_items_sn(sn)`
- `idx_ticket_items_material_code(material_code)`
- `idx_ticket_items_validation(validation_status)`

### 4.11 `workflow_statuses` 状态定义表

用途：集中维护状态码，避免状态含义散落在代码中。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 状态 ID |
| `status_code` | VARCHAR(50) | 是 |  | 状态编码 |
| `status_name` | VARCHAR(100) | 是 |  | 中文名称 |
| `status_category` | VARCHAR(30) | 是 |  | `ticket` / `email` / `reply` / `system` |
| `description` | VARCHAR(500) | 否 |  | 说明 |
| `is_terminal` | TINYINT(1) | 是 | 0 | 是否终态 |
| `sort_order` | INT | 是 | 0 | 展示排序 |
| `enabled` | TINYINT(1) | 是 | 1 | 是否启用 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |
| `updated_at` | DATETIME(3) | 是 | 当前时间 | 更新时间 |

索引：

- `uk_workflow_statuses_code(status_code)`

### 4.12 `workflow_transitions` 状态流转规则表

用途：定义允许的状态跳转。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 流转规则 ID |
| `from_status_code` | VARCHAR(50) | 是 |  | 原状态 |
| `to_status_code` | VARCHAR(50) | 是 |  | 目标状态 |
| `trigger_event` | VARCHAR(100) | 是 |  | 触发事件 |
| `condition_desc` | VARCHAR(500) | 否 |  | 条件说明 |
| `require_manual` | TINYINT(1) | 是 | 0 | 是否必须人工触发 |
| `enabled` | TINYINT(1) | 是 | 1 | 是否启用 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |
| `updated_at` | DATETIME(3) | 是 | 当前时间 | 更新时间 |

索引：

- `uk_workflow_transitions_rule(from_status_code, to_status_code, trigger_event)`
- `idx_workflow_transitions_from(from_status_code)`
- `idx_workflow_transitions_to(to_status_code)`

### 4.13 `ticket_status_logs` 工单状态流转日志

用途：每次工单状态变化必须记录。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 日志 ID |
| `ticket_id` | BIGINT UNSIGNED | 是 |  | 工单 ID |
| `from_status_code` | VARCHAR(50) | 否 |  | 原状态 |
| `to_status_code` | VARCHAR(50) | 是 |  | 目标状态 |
| `trigger_event` | VARCHAR(100) | 是 |  | 触发事件 |
| `reason` | VARCHAR(500) | 否 |  | 流转原因 |
| `operator_type` | VARCHAR(30) | 是 | `system` | `system` / `user` / `ai` |
| `operator_user_id` | BIGINT UNSIGNED | 否 |  | 操作用户 ID |
| `metadata` | JSON | 否 |  | 缺失字段、SN 校验结果等上下文 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |

索引：

- `idx_status_logs_ticket(ticket_id)`
- `idx_status_logs_created_at(created_at)`
- `idx_status_logs_transition(from_status_code, to_status_code)`
- `idx_status_logs_user(operator_user_id, created_at)`

### 4.14 `field_audit_logs` 字段修改审计表

用途：记录人工修正字段、系统采纳解析结果的前后值。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 审计 ID |
| `ticket_id` | BIGINT UNSIGNED | 是 |  | 工单 ID |
| `ticket_item_id` | BIGINT UNSIGNED | 否 |  | 工单明细 ID |
| `field_name` | VARCHAR(100) | 是 |  | 字段名 |
| `old_value` | TEXT | 否 |  | 修改前值 |
| `new_value` | TEXT | 否 |  | 修改后值 |
| `source_type` | VARCHAR(30) | 是 |  | `rule` / `ai` / `manual` / `system` |
| `reason` | VARCHAR(500) | 否 |  | 修改原因 |
| `operator_user_id` | BIGINT UNSIGNED | 否 |  | 操作人 |
| `parse_result_id` | BIGINT UNSIGNED | 否 |  | 来源解析结果 ID |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |

索引：

- `idx_field_audit_ticket(ticket_id)`
- `idx_field_audit_item(ticket_item_id)`
- `idx_field_audit_field(field_name)`
- `idx_field_audit_user(operator_user_id, created_at)`

### 4.15 `parse_results` 结构化解析结果表

用途：保存规则、附件、AI、人工解析产生的候选结果。工单表只保存采纳后的结果。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 解析结果 ID |
| `email_id` | BIGINT UNSIGNED | 是 |  | 邮件 ID |
| `source_attachment_id` | BIGINT UNSIGNED | 否 |  | 来源附件 ID，正文解析时为空 |
| `ticket_id` | BIGINT UNSIGNED | 否 |  | 关联工单 ID |
| `parser_type` | VARCHAR(30) | 是 |  | `rule` / `ai` / `manual` |
| `parser_version` | VARCHAR(50) | 否 |  | 解析器版本 |
| `intent_type` | VARCHAR(50) | 否 |  | 邮件意图 |
| `extracted_fields` | JSON | 否 |  | 抽取出的主字段 |
| `extracted_items` | JSON | 否 |  | 抽取出的多行明细 |
| `missing_fields` | JSON | 否 |  | 缺失字段列表 |
| `conflict_fields` | JSON | 否 |  | 冲突字段 |
| `confidence_score` | DECIMAL(5,4) | 否 |  | 整体置信度 |
| `field_confidences` | JSON | 否 |  | 字段级置信度 |
| `evidence` | JSON | 否 |  | 证据片段 |
| `apply_status` | VARCHAR(30) | 是 | `pending` | `pending` / `auto_applied` / `manually_applied` / `partially_applied` / `rejected` |
| `applied_by_user_id` | BIGINT UNSIGNED | 否 |  | 应用人 |
| `applied_at` | DATETIME(3) | 否 |  | 应用时间 |
| `accepted` | TINYINT(1) | 是 | 0 | 是否被采纳 |
| `accepted_by_user_id` | BIGINT UNSIGNED | 否 |  | 采纳人 |
| `accepted_at` | DATETIME(3) | 否 |  | 采纳时间 |
| `error_message` | TEXT | 否 |  | 解析错误 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |

索引：

- `idx_parse_results_email(email_id)`
- `idx_parse_results_attachment(source_attachment_id)`
- `idx_parse_results_ticket(ticket_id)`
- `idx_parse_results_parser(parser_type, parser_version)`
- `idx_parse_results_apply_status(apply_status)`
- `idx_parse_results_accepted(accepted)`

约束建议：

- `confidence_score IS NULL OR confidence_score BETWEEN 0 AND 1`

### 4.16 `sn_validation_results` SN 校验结果表

用途：记录每次 SN 与客户、物料、有效期的校验结果。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 校验结果 ID |
| `ticket_id` | BIGINT UNSIGNED | 是 |  | 工单 ID |
| `ticket_item_id` | BIGINT UNSIGNED | 否 |  | 工单明细 ID |
| `sn` | VARCHAR(100) | 是 |  | 被校验编号/SN |
| `matched_sn_asset_id` | BIGINT UNSIGNED | 否 |  | 匹配到的 SN 资产 ID |
| `check_exists` | TINYINT(1) | 否 |  | SN 是否存在 |
| `check_valid` | TINYINT(1) | 否 |  | SN 是否有效 |
| `check_customer_match` | TINYINT(1) | 否 |  | 客户是否匹配 |
| `check_material_match` | TINYINT(1) | 否 |  | 物料是否匹配 |
| `need_ship_to_beijing` | TINYINT(1) | 否 |  | 是否需要寄北京 |
| `result_status` | VARCHAR(30) | 是 |  | `pass` / `warning` / `failed` |
| `result_message` | TEXT | 否 |  | 校验说明 |
| `checked_by` | VARCHAR(30) | 是 | `system` | `system` / `manual` |
| `checked_at` | DATETIME(3) | 是 | 当前时间 | 校验时间 |

索引：

- `idx_sn_validation_ticket(ticket_id)`
- `idx_sn_validation_item(ticket_item_id)`
- `idx_sn_validation_sn(sn)`
- `idx_sn_validation_status(result_status)`
- `idx_sn_validation_asset(matched_sn_asset_id)`

### 4.17 `sn_assets` SN/资产校验库

用途：一期核心基础资料表。用于判断 SN 是否存在、是否有效、是否属于对应客户、是否匹配物料。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 资产 ID |
| `customer_code` | VARCHAR(50) | 是 |  | 客户编号 |
| `customer_name` | VARCHAR(255) | 是 |  | 客户名称 |
| `material_code` | VARCHAR(100) | 是 |  | 物料代码 |
| `material_name` | VARCHAR(255) | 否 |  | 物料名称 |
| `sn` | VARCHAR(100) | 是 |  | 编号/SN，唯一 |
| `asset_status` | VARCHAR(30) | 是 | `valid` | `valid` / `expired` / `disabled` / `unknown` |
| `warranty_start_date` | DATE | 否 |  | 有效期开始 |
| `warranty_end_date` | DATE | 否 |  | 有效期结束 |
| `source_file_name` | VARCHAR(255) | 否 |  | 来源文件名 |
| `source_file_hash` | CHAR(64) | 否 |  | 来源文件 SHA256 |
| `source_row_no` | INT | 否 |  | 来源文件行号 |
| `raw_data` | JSON | 否 |  | 原始导入行 JSON |
| `imported_by_user_id` | BIGINT UNSIGNED | 否 |  | 导入人 |
| `imported_at` | DATETIME(3) | 否 |  | 导入时间 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |
| `updated_at` | DATETIME(3) | 是 | 当前时间 | 更新时间 |

索引：

- `uk_sn_assets_sn(sn)`
- `idx_sn_assets_customer_code(customer_code)`
- `idx_sn_assets_customer_name(customer_name)`
- `idx_sn_assets_material_code(material_code)`
- `idx_sn_assets_material_name(material_name)`
- `idx_sn_assets_status(asset_status)`
- `idx_sn_assets_source(source_file_hash, source_row_no)`

### 4.18 `board_cards` 板卡/物料寄北京规则表

用途：保存唯一物料代码对应的物料名称和是否寄北京规则。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 物料/板卡 ID |
| `material_code` | VARCHAR(100) | 是 |  | 唯一物料代码/板卡编号 |
| `material_name` | VARCHAR(255) | 否 |  | 物料名称/板卡名称 |
| `need_ship_to_beijing` | TINYINT(1) | 是 | 0 | 是否寄北京 |
| `shipping_address` | VARCHAR(500) | 否 |  | 收货地址 |
| `shipping_contact` | VARCHAR(100) | 否 |  | 收货联系人 |
| `shipping_phone` | VARCHAR(100) | 否 |  | 收货电话 |
| `postal_code` | VARCHAR(20) | 否 |  | 邮编 |
| `status` | VARCHAR(20) | 是 | `active` | `active` / `inactive` |
| `source_file_name` | VARCHAR(255) | 否 |  | 来源文件名 |
| `source_file_hash` | CHAR(64) | 否 |  | 来源文件 SHA256 |
| `source_row_no` | INT | 否 |  | 来源文件行号 |
| `raw_data` | JSON | 否 |  | 原始导入行 JSON |
| `imported_by_user_id` | BIGINT UNSIGNED | 否 |  | 导入人 |
| `imported_at` | DATETIME(3) | 否 |  | 导入时间 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |
| `updated_at` | DATETIME(3) | 是 | 当前时间 | 更新时间 |

索引：

- `uk_board_cards_material_code(material_code)`
- `idx_board_cards_material_name(material_name)`
- `idx_board_cards_ship_to_beijing(need_ship_to_beijing)`
- `idx_board_cards_status(status)`
- `idx_board_cards_source(source_file_hash, source_row_no)`

### 4.19 `reply_templates` 回复话术模板表

用途：维护追问、确认、人工回复模板。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 模板 ID |
| `template_code` | VARCHAR(100) | 是 |  | 模板编码 |
| `template_name` | VARCHAR(100) | 是 |  | 模板名称 |
| `template_type` | VARCHAR(50) | 是 |  | `missing_fields` / `confirm` / `manual` |
| `language` | VARCHAR(20) | 是 | `zh-CN` | 语言 |
| `version` | VARCHAR(30) | 是 |  | 模板版本 |
| `subject_template` | VARCHAR(500) | 否 |  | 主题模板 |
| `body_template` | MEDIUMTEXT | 是 |  | 正文模板 |
| `enabled` | TINYINT(1) | 是 | 1 | 是否启用 |
| `created_by_user_id` | BIGINT UNSIGNED | 否 |  | 创建人 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |
| `updated_at` | DATETIME(3) | 是 | 当前时间 | 更新时间 |

索引：

- `uk_reply_templates_code_version(template_code, version)`
- `idx_reply_templates_type_enabled(template_type, enabled)`

### 4.20 `reply_records` 回复记录表

用途：记录追问草稿、人工审核、发送状态和外发邮件归档关系。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 回复记录 ID |
| `ticket_id` | BIGINT UNSIGNED | 是 |  | 工单 ID |
| `related_email_id` | BIGINT UNSIGNED | 否 |  | 触发回复的客户邮件 ID |
| `outgoing_email_id` | BIGINT UNSIGNED | 否 |  | 发送后归档的外发邮件 ID |
| `template_id` | BIGINT UNSIGNED | 否 |  | 使用的话术模板 ID |
| `reply_type` | VARCHAR(50) | 是 |  | `missing_fields` / `confirm` / `manual` |
| `followup_round` | INT | 是 | 1 | 追问轮次 |
| `missing_fields` | JSON | 否 |  | 本次追问缺失字段 |
| `to_addresses` | TEXT | 是 |  | 收件人 |
| `cc_addresses` | TEXT | 否 |  | 抄送人 |
| `subject` | VARCHAR(500) | 否 |  | 回复主题 |
| `draft_body` | MEDIUMTEXT | 否 |  | 回复草稿 |
| `final_body` | MEDIUMTEXT | 否 |  | 最终发送正文 |
| `generate_source` | VARCHAR(30) | 是 | `system` | `template` / `ai` / `manual` / `system` |
| `ai_call_log_id` | BIGINT UNSIGNED | 否 |  | 生成草稿的 AI 调用摘要 ID |
| `review_status` | VARCHAR(30) | 是 | `pending` | `pending` / `approved` / `rejected` / `skipped` |
| `reviewed_by_user_id` | BIGINT UNSIGNED | 否 |  | 审核人 |
| `reviewed_at` | DATETIME(3) | 否 |  | 审核时间 |
| `send_status` | VARCHAR(30) | 是 | `draft` | `draft` / `queued` / `sent` / `failed` / `cancelled` |
| `smtp_message_id` | VARCHAR(500) | 否 |  | SMTP 返回 Message-ID |
| `in_reply_to` | VARCHAR(500) | 否 |  | 外发邮件 In-Reply-To |
| `references_header` | TEXT | 否 |  | 外发邮件 References |
| `sent_at` | DATETIME(3) | 否 |  | 发送时间 |
| `error_message` | TEXT | 否 |  | 发送失败原因 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |
| `updated_at` | DATETIME(3) | 是 | 当前时间 | 更新时间 |

索引：

- `idx_reply_records_ticket(ticket_id)`
- `idx_reply_records_status(send_status)`
- `idx_reply_records_related_email(related_email_id)`
- `idx_reply_records_outgoing_email(outgoing_email_id)`
- `idx_reply_records_ticket_round(ticket_id, followup_round)`
- `idx_reply_records_review_status(review_status, created_at)`

### 4.21 `manual_review_tasks` 人工复核任务表

用途：低置信度、SN 异常、追问超限、系统异常等任务进入人工队列。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 任务 ID |
| `ticket_id` | BIGINT UNSIGNED | 是 |  | 工单 ID |
| `email_id` | BIGINT UNSIGNED | 否 |  | 关联邮件 ID |
| `task_type` | VARCHAR(50) | 是 |  | `low_confidence` / `sn_error` / `followup_limit` / `system_error` / `field_conflict` / `manual` |
| `priority` | VARCHAR(20) | 是 | `normal` | `low` / `normal` / `high` / `urgent` |
| `status` | VARCHAR(30) | 是 | `pending` | `pending` / `processing` / `resolved` / `closed` |
| `description` | TEXT | 否 |  | 任务说明 |
| `trigger_reason` | VARCHAR(500) | 否 |  | 触发原因 |
| `assigned_user_id` | BIGINT UNSIGNED | 否 |  | 分配处理人 |
| `claimed_by_user_id` | BIGINT UNSIGNED | 否 |  | 领取人 |
| `claimed_at` | DATETIME(3) | 否 |  | 领取时间 |
| `resolved_by_user_id` | BIGINT UNSIGNED | 否 |  | 完成人 |
| `resolved_at` | DATETIME(3) | 否 |  | 完成时间 |
| `resolution` | TEXT | 否 |  | 处理结果 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |
| `updated_at` | DATETIME(3) | 是 | 当前时间 | 更新时间 |

索引：

- `idx_manual_tasks_ticket(ticket_id)`
- `idx_manual_tasks_email(email_id)`
- `idx_manual_tasks_status(status)`
- `idx_manual_tasks_queue(status, priority, created_at)`
- `idx_manual_tasks_assignee(assigned_user_id, status)`

### 4.22 `notification_events` 站内通知事件表

用途：人工复核任务、待审核回复、系统异常等站内通知。前端可 SSE/WebSocket/轮询消费。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 通知 ID |
| `event_type` | VARCHAR(50) | 是 |  | `manual_review_created` / `reply_waiting_review` / `system_error` |
| `target_type` | VARCHAR(50) | 是 |  | `ticket` / `email` / `manual_task` / `reply` |
| `target_id` | BIGINT UNSIGNED | 是 |  | 目标 ID |
| `title` | VARCHAR(200) | 是 |  | 通知标题 |
| `content` | VARCHAR(1000) | 否 |  | 通知内容 |
| `priority` | VARCHAR(20) | 是 | `normal` | `low` / `normal` / `high` / `urgent` |
| `recipient_user_id` | BIGINT UNSIGNED | 否 |  | 指定接收人 |
| `recipient_role_code` | VARCHAR(50) | 否 |  | 指定角色 |
| `delivery_channel` | VARCHAR(30) | 是 | `in_app` | `in_app` / `email` / `webhook` |
| `delivery_status` | VARCHAR(30) | 是 | `pending` | `pending` / `delivered` / `failed` / `read` |
| `read_at` | DATETIME(3) | 否 |  | 已读时间 |
| `metadata` | JSON | 否 |  | 上下文 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |
| `delivered_at` | DATETIME(3) | 否 |  | 推送时间 |

索引：

- `idx_notifications_recipient(recipient_user_id, delivery_status, created_at)`
- `idx_notifications_role(recipient_role_code, delivery_status, created_at)`
- `idx_notifications_target(target_type, target_id)`
- `idx_notifications_event(event_type, created_at)`

### 4.23 `ai_call_logs` AI 调用摘要索引表

用途：数据库只保存 AI 调用摘要和日志文件定位信息，完整 prompt、输入、输出、链路细节写项目 JSONL 日志。

详细 AI 日志建议路径：

```text
logs/ai/{yyyy}/{mm}/{dd}/ai-{yyyyMMdd}.jsonl
```

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | AI 调用摘要 ID |
| `trace_id` | VARCHAR(100) | 是 |  | AI 文件日志中的链路 ID |
| `email_id` | BIGINT UNSIGNED | 否 |  | 关联邮件 ID |
| `ticket_id` | BIGINT UNSIGNED | 否 |  | 关联工单 ID |
| `call_type` | VARCHAR(50) | 是 |  | `classify_email` / `extract_fields` / `generate_reply_draft` / `review_reply` |
| `provider_name` | VARCHAR(100) | 否 |  | AI 服务商 |
| `model_name` | VARCHAR(100) | 是 |  | 模型名称 |
| `prompt_version` | VARCHAR(50) | 是 |  | 提示词版本 |
| `input_summary` | VARCHAR(1000) | 否 |  | 输入摘要，不存完整正文 |
| `output_summary` | VARCHAR(1000) | 否 |  | 输出摘要 |
| `parsed_key_result` | JSON | 否 |  | 关键结构化结果，如意图、缺失字段、主要字段 |
| `confidence_score` | DECIMAL(5,4) | 否 |  | 置信度 |
| `latency_ms` | INT | 否 |  | 耗时毫秒 |
| `status` | VARCHAR(30) | 是 | `success` | `success` / `failed` |
| `error_message` | TEXT | 否 |  | 错误摘要 |
| `log_file_path` | VARCHAR(500) | 是 |  | 详细 AI 日志文件路径 |
| `log_line_no` | BIGINT UNSIGNED | 否 |  | JSONL 行号 |
| `log_record_hash` | CHAR(64) | 否 |  | 日志行 hash |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |

索引：

- `idx_ai_logs_trace(trace_id)`
- `idx_ai_logs_email(email_id)`
- `idx_ai_logs_ticket(ticket_id)`
- `idx_ai_logs_call_type(call_type)`
- `idx_ai_logs_ticket_call_time(ticket_id, call_type, created_at)`
- `idx_ai_logs_status(status, created_at)`

### 4.24 `operation_logs` 用户操作日志表

用途：记录控制台用户的关键操作。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 操作日志 ID |
| `user_id` | BIGINT UNSIGNED | 否 |  | 操作用户 ID |
| `operation_type` | VARCHAR(100) | 是 |  | 操作类型，如 `ticket_update` / `reply_approve` / `thread_merge` |
| `target_type` | VARCHAR(50) | 是 |  | 对象类型 |
| `target_id` | BIGINT UNSIGNED | 否 |  | 对象 ID |
| `description` | VARCHAR(500) | 否 |  | 操作说明 |
| `before_data` | JSON | 否 |  | 操作前数据 |
| `after_data` | JSON | 否 |  | 操作后数据 |
| `ip_address` | VARCHAR(64) | 否 |  | IP 地址 |
| `user_agent` | VARCHAR(500) | 否 |  | User-Agent |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |

索引：

- `idx_operation_logs_user(user_id, created_at)`
- `idx_operation_logs_target(target_type, target_id)`
- `idx_operation_logs_type(operation_type, created_at)`

### 4.25 `system_event_logs` 系统事件日志表

用途：记录系统异常和运行事件，区别于用户操作日志。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 系统日志 ID |
| `event_type` | VARCHAR(100) | 是 |  | `imap_error` / `smtp_error` / `oss_error` / `db_error` / `service_start` |
| `severity` | VARCHAR(20) | 是 | `info` | `debug` / `info` / `warning` / `error` / `critical` |
| `module_name` | VARCHAR(100) | 是 |  | 模块名 |
| `correlation_id` | VARCHAR(100) | 否 |  | 链路 ID |
| `email_id` | BIGINT UNSIGNED | 否 |  | 邮件 ID |
| `ticket_id` | BIGINT UNSIGNED | 否 |  | 工单 ID |
| `job_run_id` | BIGINT UNSIGNED | 否 |  | 任务运行 ID |
| `message` | VARCHAR(1000) | 是 |  | 日志摘要 |
| `details` | JSON | 否 |  | 详细上下文 |
| `stack_trace` | MEDIUMTEXT | 否 |  | 异常堆栈 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |

索引：

- `idx_system_logs_type_time(event_type, created_at)`
- `idx_system_logs_severity(severity, created_at)`
- `idx_system_logs_correlation(correlation_id)`
- `idx_system_logs_email(email_id)`
- `idx_system_logs_ticket(ticket_id)`
- `idx_system_logs_job(job_run_id)`

### 4.26 `job_run_logs` 后台任务运行日志

用途：记录 IMAP 拉取、邮件解析、回复发送、OSS 上传、数据导入等后台任务运行情况。原 `email_fetch_batches` 和 `import_batches` 的一期审计职责由本表承担。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | BIGINT UNSIGNED | 是 | 自增 | 任务运行 ID |
| `job_name` | VARCHAR(100) | 是 |  | 任务名称 |
| `job_type` | VARCHAR(50) | 是 |  | `imap_fetch` / `parse_email` / `send_reply` / `oss_upload` / `master_data_import` / `backup` |
| `status` | VARCHAR(30) | 是 | `running` | `running` / `success` / `failed` / `partial` |
| `started_at` | DATETIME(3) | 是 | 当前时间 | 开始时间 |
| `finished_at` | DATETIME(3) | 否 |  | 结束时间 |
| `duration_ms` | INT | 否 |  | 耗时毫秒 |
| `processed_count` | INT | 是 | 0 | 处理数量 |
| `success_count` | INT | 是 | 0 | 成功数量 |
| `failed_count` | INT | 是 | 0 | 失败数量 |
| `error_message` | TEXT | 否 |  | 错误信息 |
| `metadata` | JSON | 否 |  | 任务参数和上下文 |
| `created_at` | DATETIME(3) | 是 | 当前时间 | 创建时间 |

索引：

- `idx_job_run_logs_name_time(job_name, started_at)`
- `idx_job_run_logs_type_time(job_type, started_at)`
- `idx_job_run_logs_status(status, started_at)`

## 5. 关键关系说明

### 5.1 邮件、附件、OSS 的关系

```text
emails 1 - N email_attachments
email_attachments N - 1 oss_objects
emails.raw_eml_oss_object_id N - 1 oss_objects
```

含义：

- 每封邮件一行 `emails`。
- 每个附件一行 `email_attachments`。
- 附件文件本体在 OSS，`email_attachments.oss_object_id` 指向 `oss_objects.id`。
- 原始 EML 文件也在 OSS，`emails.raw_eml_oss_object_id` 指向 `oss_objects.id`。

### 5.2 回复链邮件如何保存

回复链中的多封邮件不拼成一个正文字段。

保存方式：

- 每封邮件独立存入 `emails`。
- 每封邮件保留自己的 `message_id`, `in_reply_to`, `references_header`, `subject`, `clean_body`。
- 同一回复链的邮件通过 `emails.thread_id` 关联到同一个 `email_threads.id`。
- 邮件唯一去重以 `message_id_hash` 为主依据；`duplicate_of_email_id` 为非关键兼容字段，不作为一期重复邮件处理主流程。
- 页面展示时按线程查询所有邮件并按时间排序。

### 5.3 邮件与工单如何关联

两层关联：

```text
repair_tickets.thread_id -> email_threads.id
email_ticket_links.email_id -> emails.id
email_ticket_links.ticket_id -> repair_tickets.id
```

含义：

- 工单主表通过 `thread_id` 绑定一个邮件线程。
- `email_ticket_links` 精确记录每封邮件与工单关系。
- `link_type = source` 表示来源邮件。
- `link_type = reply` 表示客户补充邮件。
- `link_type = outbound` 表示系统外发追问邮件。
- `link_type = manual` 表示人工关联。

### 5.4 客户信息保存和 SN 客户字段对应

从邮件获取的客户信息保存在 `repair_tickets`：

- `customer_code`
- `customer_name`
- `contact_person`
- `contact_phone`
- `contact_email`
- `mailing_address`

SN 库客户信息保存在 `sn_assets`：

- `customer_code`
- `customer_name`
- `material_code`
- `material_name`
- `sn`

校验时比较：

```text
repair_tickets.customer_code/name
repair_ticket_items.material_code/name
repair_ticket_items.sn
```

与：

```text
sn_assets.customer_code/name
sn_assets.material_code/name
sn_assets.sn
```

不一致时写入 `sn_validation_results`，并触发 `manual_review`。

## 6. 后续暂不进入一期的表

| 表 | 后续加入条件 |
| --- | --- |
| `mail_accounts` | 需要控制台维护多个邮箱账号时 |
| `email_fetch_batches` | `job_run_logs` 无法满足拉取审计时 |
| `import_batches` | 基础资料导入审计复杂到需要独立批次管理时 |
| `customers` | 需要独立客户主数据、联系人、SLA、权限时 |
| `customer_aliases` | 客户名称匹配需要别名体系时 |
| `board_card_aliases` | 物料唯一编号无法覆盖实际别名问题时 |
| `ai_execution_traces` | 需要在数据库中查询 AI 多步骤链路时 |
| `ai_execution_steps` | 同上 |
| `export_records` | 开始真实导出、SAP 或 IT 中转对接时 |
