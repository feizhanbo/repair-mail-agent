# 邮件报修自动化系统详细开发设计文档

生成日期：2026-07-01  
适用阶段：一期内部试运行系统  
文档用途：指导后端、前端、数据库、接口、部署、测试与 AI 辅助开发过程

## 当前实现基线（2026-07-05）

本文件保留详细设计原则。当前代码事实以 README、AI 开发进度文档和 `docs/alignment-audit/` 为准：

- 后端已实现用户管理、个人信息、站内消息、DeepSeek AI、`parse_results.apply_status` 和接口级 RBAC。
- 前端已实现用户管理、个人信息、站内消息，并按角色隐藏菜单和高危操作。
- 当前角色为 `admin/supervisor/operator`，旧 `viewer` 不再作为开发方向。
- 数据库最新迁移为 `9d2b7c4f1a30`，业务表数量为 26。
- 真实 IMAP/SMTP/OSS 尚未正式联调，自动发送默认关闭。
- 行级工单/邮件数据权限尚未建模，后续需单独设计。

## 1. 文档依据与优先级

本开发文档用于把现有 PRD、数据库一期最终版和标准研发部署流程整合为可执行的工程开发说明。所有开发人员和 AI 辅助开发工具必须按以下优先级理解需求：

| 优先级 | 文件 | 用途 |
| --- | --- | --- |
| 1 | `邮件报修自动化系统PRD技术方案.md` | 业务范围、一期边界、技术方向、数据库设计标准的最高依据 |
| 2 | `邮件报修自动化系统数据库表字段设计方案_一期最终版.md` | 一期 26 张表的字段、索引、关系、状态枚举细节参考 |
| 3 | `邮件报修自动化项目计划.docx` | 排期、验收节奏、项目风险和管理节奏参考 |
| 4 | `Codex_Docker_CICD_标准研发与部署流程.md` | 全项目强制研发、Docker、CI/CD、部署和日志规范 |

冲突处理规则：

- 数据库设计以 PRD 为主；PRD 未展开字段细节时，以数据库一期最终版补足。
- 若数据库一期最终版与 PRD 冲突，实施时按 PRD 修改数据库设计和迁移。
- 普通版、评审修订版数据库文档只能作为历史参考，不作为一期开发依据。
- 所有远程服务器研发与部署必须服从 `Codex_Docker_CICD_标准研发与部署流程.md`，不得绕过 Docker、Nginx、GitHub Actions 和日志约束。

## 2. 项目目标与一期边界

### 2.1 业务目标

系统面向客户邮件报修场景，目标是在三个月内完成可内部试运行的邮件报修自动化系统。核心闭环如下：

```text
邮件接收 -> 原文归档 -> 回复链识别 -> 模板/附件解析 -> AI 辅助判断
-> SN 校验 -> 自动追问/人工复核 -> 工单沉淀 -> 可导出
```

系统必须解决以下问题：

- 从报修邮箱自动拉取客户邮件，保存原始 EML、邮件正文、HTML、邮件头、附件元数据和 OSS 对象引用。
- 识别新报修、客户补充、内部转发、无关邮件和无法判断邮件。
- 支持标准模板、正文自由文本、历史回复链、Excel/TXT/PDF/DOCX 附件中的报修信息提取。
- 以 SN/资产校验库判断 SN 是否存在、有效、与客户及物料匹配。
- 缺失关键信息时生成追问草稿，默认必须人工审核后发送。
- 低置信度、SN 异常、字段冲突、追问超限、系统异常进入人工复核队列。
- 控制台可追溯邮件原文、附件、解析候选、AI 摘要、SN 校验、状态流转和人工修改记录。

### 2.2 一期范围

一期必须完成：

- 后端 FastAPI 服务、数据库迁移、API、后台任务、日志审计。
- 前端 React 控制台、登录鉴权、核心业务页面和通知能力。
- MySQL 8.x 一期 26 张表。
- IMAP 收信、SMTP 发信能力，默认关闭真实自动发送。
- 阿里云 OSS 文件存储，数据库不保存附件或原始 EML 二进制。
- APScheduler 轻量任务调度。
- AI Provider 抽象层，支持外部 API 与后续内网模型切换。
- Docker Compose、Nginx、GitHub Actions、部署脚本、日志查看和基础监控结构。
- README 与 AI 开发进度文档同步维护机制。

一期不做：

- SAP 或 IT 中转服务器真实对接。
- 多邮箱账号控制台维护。
- 独立客户主数据体系。
- 企业微信、邮件、Webhook 等外部通知集成。
- 高可用集群、复杂分布式任务队列和多租户权限体系。

## 3. 项目初始化文档要求

初始创建项目时，仓库根目录必须包含以下两份常规文档。这两份文档是研发过程的一部分，不能等到交付前补写。

### 3.1 `README.md`

`README.md` 面向新加入的团队成员、运维人员和项目验收人员，必须包含以下章节：

```text
# 邮件报修自动化系统

## 项目简介
说明业务背景、目标用户、一期内部试运行定位。

## 核心业务流程
说明邮件接收、解析、SN 校验、追问、人工复核、工单沉淀、导出预留。

## 技术栈
列出后端、前端、数据库、任务调度、文件存储、AI、部署组件。

## 项目结构
说明 backend、frontend、infra、nginx、docker、.github/workflows、docs 等目录职责。

## 环境变量
罗列 DATABASE_URL、JWT、IMAP、SMTP、OSS、AI、AUTO_SEND_ENABLED、MAX_FOLLOW_UP、CONFIDENCE_THRESHOLD 等配置。

## 本地启动
提供后端、前端、数据库迁移和 Docker Compose 启动命令。

## 数据库迁移
提供 Alembic 生成、升级、回滚和种子数据初始化命令。

## 测试命令
提供后端单元测试、接口测试、前端测试和 E2E 测试命令。

## Docker 部署
说明镜像构建、docker compose up/down、日志查看。

## CI/CD
说明 GitHub Actions 触发分支、部署流程和服务器脚本。

## 日志与排障
说明 API logs、Nginx logs、后台任务日志、AI JSONL 日志位置。

## 常见问题
记录邮件未入库、OSS 上传失败、AI 调用失败、SMTP 发送失败、迁移失败等处理方式。
```

### 3.2 `AI开发进度与任务跟踪.md`

该文档面向 AI 辅助开发，用于在长周期开发中保持方向一致。每次完成模块、接口、数据库迁移、页面、测试或部署配置后，必须更新本文件。

推荐结构：

```text
# AI开发进度与任务跟踪

## 1. PRD 摘要
记录系统目标、一期边界、关键默认决策。

## 2. 当前开发阶段
记录当前周次、阶段目标、正在开发的模块、最新完成事项。

## 3. 全量任务树
按后端、前端、数据库、接口、部署、测试、文档拆分任务。

## 4. 模块完成度
使用状态：未开始 / 进行中 / 已完成 / 阻塞 / 暂缓。

## 5. 数据库完成度
记录 26 张表的 ORM、迁移、索引、种子数据、测试完成情况。

## 6. API 完成度
记录每个 endpoint 的 DTO、业务逻辑、权限、测试和前端联调状态。

## 7. 前端页面完成度
记录页面、组件、状态管理、接口联调和 E2E 测试状态。

## 8. 测试与验收进度
记录单元测试、集成测试、真实邮件回归、部署验收情况。

## 9. 当前阻塞项
记录缺失资料、环境问题、业务冲突和技术债。

## 10. 下一步开发约束
明确下一轮 AI 开发必须遵守的范围、禁止修改项和验收标准。
```

维护规则：

- 状态必须真实，不允许把未验收功能标记为已完成。
- 每次 AI 开发前必须先读取该文档，确认当前阶段和禁止偏离事项。
- 每次 AI 开发结束必须更新已完成、未完成、阻塞和下一步。
- 若代码与 PRD 或数据库一期最终版冲突，进度文档必须记录冲突和修正方向。

## 4. 标准研发与部署流程执行规范

所有开发过程严格执行 `Codex_Docker_CICD_标准研发与部署流程.md`。

### 4.1 强制约束

- 所有服务必须 Docker 化。
- 所有外部请求必须通过 Nginx 入口。
- 所有部署必须通过 GitHub Actions。
- 禁止手动在生产环境直接修改代码。
- 所有日志必须可追踪。
- 所有服务必须可重建，业务服务保持无状态，文件进入 OSS，持久数据进入 MySQL。

### 4.2 标准项目结构

仓库推荐结构：

```text
project/
  README.md
  AI开发进度与任务跟踪.md
  backend/
    app/
      api/v1/
      core/
      models/
      schemas/
      repositories/
      services/
      workers/
      integrations/
      middleware/
      main.py
      config.py
    alembic/
    requirements.txt
    Dockerfile
  frontend/
    src/
      api/
      assets/
      components/
      layouts/
      pages/
      routes/
      stores/
      styles/
      types/
      utils/
    package.json
    vite.config.ts
  infra/
    docker-compose.yml
    backup/
  nginx/
    default.conf
  docker/
    backend.Dockerfile
    frontend.Dockerfile
  deploy.sh
  .github/workflows/
    deploy.yml
```

若当前仓库已存在早期结构，实施时按上述结构逐步对齐；对齐过程必须记录在 `AI开发进度与任务跟踪.md`。

### 4.3 开发流程

每个功能模块按以下步骤开发：

1. 阅读 PRD、数据库一期最终版、当前 AI 进度文档。
2. 明确本次改动范围和验收标准。
3. 编写或修改代码、迁移、测试和文档。
4. 本地或 Docker 环境验证。
5. 更新 `README.md` 和 `AI开发进度与任务跟踪.md`。
6. 提交代码并推送。
7. GitHub Actions 自动部署。
8. 通过 Nginx 入口访问服务。
9. 查看 Docker logs、Nginx logs、系统事件日志确认运行状态。

## 5. 总体技术架构

### 5.1 技术栈

| 层级 | 选型 | 说明 |
| --- | --- | --- |
| 后端语言 | Python 3.11+ | 与 FastAPI、SQLAlchemy asyncio、AI/邮件生态匹配 |
| 后端框架 | FastAPI | REST API、OpenAPI 文档、Pydantic 校验 |
| ORM | SQLAlchemy 2.x asyncio | 异步 MySQL 访问 |
| 数据库驱动 | asyncmy | MySQL 异步驱动 |
| 数据库 | MySQL 8.x | 字符集 `utf8mb4`，时间字段 `DATETIME(3)` |
| 迁移 | Alembic | 所有表结构变更必须迁移化 |
| 配置 | pydantic-settings + `.env` | 环境变量集中管理 |
| 任务调度 | APScheduler | 一期轻量后台任务 |
| 文件存储 | 阿里云 OSS | 原始 EML、附件、导出文件 |
| 邮件接入 | IMAP/SMTP | IMAP 收信，SMTP 发信 |
| AI 接入 | 统一 AI Provider | 外部 API 与内网模型可切换 |
| 前端 | React + TypeScript + Vite | 控制台单页应用 |
| UI | Ant Design | 表格、表单、抽屉、消息、布局 |
| 前端请求 | TanStack Query + Axios/fetch 封装 | 服务端状态缓存、重试和刷新 |
| 前端本地状态 | Zustand | 用户、筛选器、布局状态 |
| 部署 | Docker Compose + Nginx + GitHub Actions | 标准化部署 |

### 5.2 后端模块划分

| 模块 | 职责 | 主要依赖 | 主要数据表 |
| --- | --- | --- | --- |
| `auth` | 登录、JWT、角色权限、当前用户 | users/roles repository | `users`, `roles`, `user_roles` |
| `mail_ingest` | IMAP 拉取、去重、EML 入 OSS、邮件入库 | OSS、IMAP、job logger | `emails`, `email_attachments`, `oss_objects`, `job_run_logs` |
| `mail_threading` | 回复链识别、线程归并、人工合并/拆分 | email repository | `email_threads`, `email_ticket_links` |
| `parser` | 正文清洗、模板解析、附件解析、字段标准化 | attachment parser、AI Provider | `parse_results`, `email_attachments` |
| `ai_provider` | 分类、字段抽取、追问草稿、回复审查 | httpx、日志 | `ai_call_logs` + JSONL |
| `sn_registry` | SN/资产库导入、板卡规则导入、SN 校验 | Excel/CSV parser | `sn_assets`, `board_cards`, `sn_validation_results` |
| `ticket_workflow` | 工单生成、状态机、乐观锁、状态日志 | parser、SN、review、reply | `repair_tickets`, `repair_ticket_items`, `workflow_*`, `ticket_status_logs` |
| `reply` | 追问模板、草稿、审核、SMTP 发送、外发归档 | SMTP、mail_ingest、AI | `reply_templates`, `reply_records`, `emails` |
| `manual_review` | 人工任务创建、领取、分配、完成 | notification、workflow | `manual_review_tasks`, `notification_events` |
| `notification` | 站内通知、SSE/轮询接口 | manual_review、reply、system logs | `notification_events` |
| `observability` | 操作日志、系统事件、后台任务日志、AI 日志索引 | middleware、services | `operation_logs`, `system_event_logs`, `job_run_logs`, `ai_call_logs` |
| `system_config` | 系统配置读写、运行开关、阈值查询 | settings、operation log | `.env`、配置表后续扩展 |

### 5.3 模块间通信机制

- API 层只负责鉴权、参数校验、调用 service、返回 DTO。
- Service 层承载业务流程，不直接拼 SQL。
- Repository 层封装数据库访问，禁止 API 层直接访问 ORM session 做复杂查询。
- 后台任务通过 service 调用业务模块，任务开始和结束必须写 `job_run_logs`。
- 模块内同步调用使用 Python service 方法；跨进程异步消息一期不引入。
- 人工复核任务创建时必须同时写 `manual_review_tasks` 和 `notification_events`。
- 状态流转必须调用 `ticket_workflow.transition()`，前端和其他模块不得直接更新 `repair_tickets.current_status_code`。
- 字段修正必须写 `field_audit_logs`，同时写 `operation_logs`。

### 5.4 依赖方向

依赖方向必须稳定：

```text
api -> services -> repositories -> models
workers -> services
services -> integrations
services -> observability
models 不依赖 services
repositories 不依赖 api
integrations 不依赖业务 service
```

禁止事项：

- 禁止在 API 路由中直接编写复杂业务流程。
- 禁止在 ORM model 中调用外部接口。
- 禁止 AI 输出直接覆盖人工锁定字段。
- 禁止绕过状态机直接更新状态。
- 禁止附件二进制写入数据库。

## 6. 后端开发设计

### 6.1 配置文件管理策略

配置统一由 `Settings` 管理，从 `.env`、容器环境变量和 CI/CD secret 注入。

必备配置：

| 配置 | 示例 | 说明 |
| --- | --- | --- |
| `APP_ENV` | `dev` / `test` / `prod` | 运行环境 |
| `DATABASE_URL` | `mysql+asyncmy://user:pass@mysql:3306/repair_system` | 数据库连接 |
| `JWT_SECRET` | 生产环境必须使用强随机值 | JWT 密钥 |
| `JWT_ALGORITHM` | `HS256` | JWT 算法 |
| `JWT_EXPIRE_MINUTES` | `480` | 登录有效期 |
| `IMAP_HOST` / `IMAP_PORT` / `IMAP_USER` / `IMAP_PASSWORD` | 由运维提供 | 收信配置 |
| `SMTP_HOST` / `SMTP_PORT` / `SMTP_USER` / `SMTP_PASSWORD` | 由运维提供 | 发信配置 |
| `OSS_ENDPOINT` / `OSS_BUCKET` / `OSS_ACCESS_KEY` / `OSS_SECRET_KEY` | 由运维提供 | OSS 配置 |
| `AI_PROVIDER` / `AI_API_KEY` / `AI_MODEL` / `AI_BASE_URL` | 由运维提供 | AI Provider 配置 |
| `AUTO_SEND_ENABLED` | `false` | 默认关闭 AI 自动发送 |
| `REPLY_SEND_MODE` | `human_review` | 当前测试环境默认人工确认后发送 |
| `AUTO_SEND_MIN_CONFIDENCE` | `0.85` | 自动发送最低置信度 |
| `MAX_FOLLOW_UP` | `3` | 追问上限 |
| `CONFIDENCE_THRESHOLD` | `0.7` | 自动应用阈值 |

配置规则：

- `.env.example` 必须列出所有配置，但不能包含真实密钥。
- 生产密钥只通过服务器环境变量或 GitHub Actions Secrets 注入。
- `AUTO_SEND_ENABLED=false` 时不触发 AI 自动发送；当前实现中，人工审核通过后会进入发送流程，是否成功取决于 SMTP 配置和发送结果。
- 配置变更必须写 `system_event_logs` 或 `operation_logs`。

### 6.2 数据访问规范

- 每个数据库表对应一个 SQLAlchemy ORM model。
- 每个业务聚合建立 repository，例如 `EmailRepository`、`TicketRepository`、`ReviewRepository`。
- 写操作必须处在事务中；同一业务流程涉及多表写入时使用同一个 session。
- 列表查询必须分页，默认 `page=1&page_size=20`，最大 `page_size=100`。
- 所有时间字段使用服务端时间，字段类型按一期最终版使用 `DATETIME(3)`。
- JSON 字段只存结构化对象，不存未解析的大型正文。
- 查询条件涉及状态、时间、外键时必须命中一期最终版定义的索引。

### 6.3 后端目录规范

```text
backend/app/
  api/v1/
    auth.py
    dashboard.py
    emails.py
    email_threads.py
    tickets.py
    parse_results.py
    manual_review.py
    replies.py
    master_data.py
    ai_logs.py
    notifications.py
    system.py
  core/
    security.py
    permissions.py
    database.py
    exceptions.py
    logging.py
  models/
  schemas/
  repositories/
  services/
  workers/
  integrations/
    imap_client.py
    smtp_client.py
    oss_client.py
    ai_client.py
  middleware/
```

命名约定：

- ORM 类使用单数名，例如 `RepairTicket`。
- 表名使用一期最终版表名，例如 `repair_tickets`。
- Pydantic schema 使用 `Create`、`Update`、`Read`、`ListItem`、`Detail` 后缀。
- Service 方法使用业务动词，例如 `create_ticket_from_parse_result()`、`transition_ticket()`。
- API 文件名使用复数资源名。

### 6.4 核心模块开发细则

#### 6.4.1 认证与权限

功能：

- 用户使用用户名和密码登录。
- 密码只保存哈希。
- JWT 中包含 `sub`、`user_id`、`roles`、`exp`。
- 所有业务 API 默认需要登录，健康检查和登录接口除外。
- 角色包括 `admin`、`supervisor`、`operator`。`admin` 为系统管理员，`supervisor` 为业务主管和审核者，`operator` 为一线处理人员。

验收：

- 禁用用户不能登录。
- 过期 token 被拒绝。
- 只读用户不能执行字段修改、状态流转、审核发送、导入基础资料。

#### 6.4.2 邮件接入 `mail_ingest`

处理流程：

1. 创建 `job_run_logs`，`job_type=imap_fetch`，状态 `running`。
2. 连接 IMAP，按文件夹和 UID 拉取新邮件。
3. 解析邮件头、正文、HTML、附件、Message-ID、In-Reply-To、References。
4. 计算 `message_id_hash`，命中 `uk_emails_message_id_hash` 时标记重复。
5. 原始 EML 上传 OSS，写 `oss_objects`。
6. 每个附件上传 OSS，写 `oss_objects` 和 `email_attachments`。
7. 写 `emails`，方向为 `inbound`。
8. 调用 `mail_threading` 归并线程。
9. 更新 `job_run_logs` 统计数量和状态。

异常处理：

- IMAP 连接失败写 `system_event_logs(event_type=imap_error)`。
- OSS 上传失败时 `oss_objects.upload_status=failed`，邮件进入 `error` 或附件 `parse_status=failed`。
- 单封邮件失败不能中断整个批次，批次状态为 `partial`。

#### 6.4.3 邮件线程 `mail_threading`

归并依据按优先级：

1. `In-Reply-To` 或 `References` 命中已入库邮件。
2. SMTP 外发追问的 `Message-ID` 与客户回复头匹配。
3. 归一化主题相同、发件人域名相同、时间窗口合理。
4. 正文或附件中 SN 与已有工单高度一致。

规则：

- 自动归并要写 `email_threads.merge_confidence` 和 `merge_reason`。
- `manual_locked=1` 的线程不允许自动拆分或重归并。
- 人工合并/拆分必须写 `operation_logs`。

#### 6.4.4 解析引擎 `parser`

解析来源：

- 邮件最新回复段。
- 邮件清洗正文。
- 历史回复链中可补充的信息。
- Excel、TXT、PDF、DOCX 附件提取文本或结构化内容。
- AI 结构化输出。

输出必须写入 `parse_results`：

- `parser_type`: `rule` / `ai` / `manual`
- `parser_version`
- `intent_type`
- `extracted_fields`
- `extracted_items`
- `missing_fields`
- `conflict_fields`
- `confidence_score`
- `field_confidences`
- `evidence`
- `apply_status`

自动应用条件：

- 意图为 `new_repair` 或 `customer_reply`。
- 关键字段完整，或缺失字段可明确追问。
- 整体置信度大于等于 `CONFIDENCE_THRESHOLD`。
- 未触碰人工锁定字段。
- SN 校验结果不是 `failed`。

低置信度、字段冲突、SN 不匹配时，不自动覆盖工单，必须创建人工任务。

#### 6.4.5 AI Provider

统一接口：

```text
classify_email(input) -> intent_type, confidence, evidence
extract_fields(input) -> extracted_fields, extracted_items, missing_fields, confidence
generate_reply_draft(input) -> subject, body, missing_fields
review_reply(input) -> risk_level, suggestions
```

日志规则：

- 数据库 `ai_call_logs` 只保存摘要、模型、耗时、状态、置信度和 JSONL 定位。
- 完整 prompt、输入、输出写入 `logs/ai/{yyyy}/{mm}/{dd}/ai-{yyyyMMdd}.jsonl`。
- 日志行必须包含 `trace_id`、`call_type`、`prompt_version`、`input`、`output`、`latency_ms`、`status`。
- AI 调用失败不能导致邮件丢失；失败要写系统日志并进入人工复核或规则兜底。

安全规则：

- AI 不作为唯一事实来源。
- AI 输出必须通过 Pydantic schema 校验。
- AI 不允许静默覆盖 `manual_locked=1` 的字段。
- AI 生成追问只能作为草稿。

#### 6.4.6 SN 校验与基础资料

导入：

- 当前公共导入导出主流程使用 XLSX；CSV 辅助函数保留为兼容和单元测试工具。
- 导入 SN 库写入 `sn_assets`。
- 导入板卡寄北京规则写入 `board_cards`。
- 每行保留 `source_file_name`、`source_file_hash`、`source_row_no`、`raw_data`。
- 导入任务写 `job_run_logs(job_type=master_data_import)`。

校验：

- 检查 SN 是否存在。
- 检查资产状态是否有效。
- 检查客户编号或客户名称是否匹配。
- 检查物料代码或物料名称是否匹配。
- 检查是否需要寄北京。
- 结果写 `sn_validation_results`，并更新 `repair_ticket_items.validation_status`。

触发人工复核：

- SN 不存在。
- SN 过期或禁用。
- 客户不匹配。
- 物料不匹配。
- 多个候选无法确定。

#### 6.4.7 工单工作流

状态：

```text
new_email
parsed
need_customer_info
auto_replied
manual_review
ready_for_export
error
closed
```

状态流转必须由 `workflow_transitions` 定义，由 `ticket_workflow.transition_ticket()` 执行。每次流转写 `ticket_status_logs`。

主要流转：

| 来源 | 目标 | 触发事件 | 条件 |
| --- | --- | --- | --- |
| `new_email` | `parsed` | `parse_completed` | 至少产生一个解析结果 |
| `parsed` | `ready_for_export` | `validation_passed` | 字段完整且 SN 校验通过 |
| `parsed` | `need_customer_info` | `missing_fields_detected` | 关键字段缺失 |
| `need_customer_info` | `auto_replied` | `reply_draft_created` | 已生成追问草稿或已发送 |
| `auto_replied` | `parsed` | `customer_reply_received` | 客户回复被归并到原线程 |
| 任意非终态 | `manual_review` | `manual_review_required` | 低置信度、SN 异常、冲突、超限、系统异常 |
| `manual_review` | `ready_for_export` | `manual_resolved` | 人工修正且校验通过 |
| 任意非终态 | `error` | `system_error` | 系统异常 |
| 任意非终态 | `closed` | `manual_close` | 人工关闭 |

#### 6.4.8 自动回复与审核

默认策略：

- 当前测试配置默认 `reply_send_mode=human_review`，AI/模板生成回复草稿后进入人工确认。
- `AUTO_SEND_ENABLED=false` 时不触发 AI 自动发送；人工审核通过后调用发送流程，SMTP 未配置或发送失败时记录失败并转人工任务。
- 追问上限默认 3 次，超过后进入 `manual_review`，任务类型 `followup_limit`。

生成流程：

1. 根据 `repair_tickets.missing_fields` 选择 `reply_templates`。
2. 可调用 AI 生成更自然的草稿。
3. 写 `reply_records`，`review_status=pending`，`send_status=pending_review`。
4. 通过人工任务和通知提示人工确认回复草稿。
5. 若配置为 `auto_send` 且满足置信度、收件人和风险条件，自动审核并调用 SMTP；否则等待人工审核。
6. 审核通过后调用 SMTP，记录 `smtp_message_id` 和发送状态；外发邮件入库归档当前代码尚未补齐，仍作为后续业务完整性项。

#### 6.4.9 人工复核

任务来源：

- 解析低置信度。
- SN 异常。
- 追问超限。
- 系统异常。
- 字段冲突。
- 人工主动创建。

任务操作：

- 领取。
- 分配。
- 修改字段。
- 应用解析结果。
- 退回重新解析。
- 生成追问。
- 关闭工单。
- 完成任务。

所有操作必须写 `operation_logs`；字段修改必须写 `field_audit_logs`。

## 7. 数据库设计规范

### 7.1 一期数据库范围

一期数据库共 26 张表，以 PRD 和数据库一期最终版为准：

| 分组 | 表 |
| --- | --- |
| 用户权限 | `users`, `roles`, `user_roles` |
| OSS 文件 | `oss_objects` |
| 邮件归档 | `email_threads`, `emails`, `email_attachments`, `email_ticket_links` |
| 工单 | `repair_tickets`, `repair_ticket_items` |
| 状态机 | `workflow_statuses`, `workflow_transitions`, `ticket_status_logs`, `field_audit_logs` |
| 解析校验 | `parse_results`, `sn_validation_results` |
| 基础资料 | `sn_assets`, `board_cards` |
| 回复 | `reply_templates`, `reply_records` |
| 人工复核 | `manual_review_tasks`, `notification_events` |
| AI 日志 | `ai_call_logs` |
| 日志 | `operation_logs`, `system_event_logs`, `job_run_logs` |

后续暂不进入一期：`mail_accounts`、`email_fetch_batches`、`import_batches`、`customers`、`customer_aliases`、`board_card_aliases`、`ai_execution_traces`、`ai_execution_steps`、`export_records`。

### 7.2 数据库基础规范

- 数据库：MySQL 8.x。
- 字符集：`utf8mb4`。
- 排序规则：优先 `utf8mb4_unicode_ci`。
- 主键：`BIGINT UNSIGNED AUTO_INCREMENT`。
- 时间：`DATETIME(3)`。
- 状态码：`VARCHAR`，不使用数据库 `ENUM`。
- 置信度：`DECIMAL(5,4)`，范围 0 到 1。
- JSON：用于解析结果、字段置信度、证据、日志上下文、原始导入行。
- 附件和 EML：只存 OSS 引用，不存二进制。
- 审计：字段修改、状态流转、用户操作、系统事件、后台任务都要留痕。

### 7.3 关键关系

邮件、附件、OSS：

```text
emails 1 - N email_attachments
email_attachments N - 1 oss_objects
emails.raw_eml_oss_object_id N - 1 oss_objects
```

邮件、线程、工单：

```text
emails.thread_id -> email_threads.id
repair_tickets.thread_id -> email_threads.id
email_ticket_links.email_id -> emails.id
email_ticket_links.ticket_id -> repair_tickets.id
```

工单、明细、SN：

```text
repair_tickets 1 - N repair_ticket_items
repair_ticket_items.sn_asset_id -> sn_assets.id
sn_validation_results.ticket_item_id -> repair_ticket_items.id
```

人工复核和通知：

```text
manual_review_tasks.ticket_id -> repair_tickets.id
notification_events.target_type + target_id -> ticket/email/manual_task/reply
```

### 7.4 表实现要求

实施时必须逐表满足数据库一期最终版字段、索引和关系。核心要求：

- `emails.message_id_hash` 唯一，用于邮件去重。
- `email_threads.thread_key` 唯一，用于线程归并。
- `repair_tickets.ticket_no` 唯一，用于业务展示和外部沟通。
- `repair_tickets.version` 用于乐观锁。
- `repair_tickets.manual_locked` 和 `repair_ticket_items.manual_locked` 用于防止自动覆盖人工确认结果。
- `repair_tickets.missing_fields` 和 `conflict_fields` 使用 JSON。
- `parse_results` 保存所有解析候选，不直接等同于工单当前值。
- `reply_records` 同时覆盖草稿、审核、发送和外发邮件归档关系。
- `ai_call_logs` 保存摘要和 JSONL 定位，不保存完整 prompt 与完整邮件正文。
- `job_run_logs` 承担一期 IMAP 拉取、导入、发送、备份等任务审计职责。

### 7.5 迁移规范

- 所有表结构变更必须通过 Alembic migration。
- 首个迁移创建 26 张一期表、索引、外键和约束。
- 种子数据必须初始化 `workflow_statuses`、`workflow_transitions`、`roles`、默认管理员用户和基础回复模板。
- 每个迁移文件必须可正向升级、可回滚。
- 已上线字段不能直接删除；如需废弃，先停止使用并记录迁移说明。
- 迁移执行前必须备份数据库。

当前代码若存在早期表名，实施时必须迁移对齐：

| 早期命名 | PRD 标准命名 |
| --- | --- |
| `tickets` | `repair_tickets` |
| `attachments` | `email_attachments` |
| `auto_replies` | `reply_records` |
| `audit_logs` | `operation_logs` 或 `field_audit_logs` |
| `system_logs` | `system_event_logs` |
| `sn_library` | `sn_assets` |
| `ai_execution_logs` | `ai_call_logs` |

## 8. RESTful API 设计规范

### 8.1 URL 命名

- API 前缀统一为 `/api/v1`。
- 资源使用复数名词，例如 `/emails`、`/tickets`、`/manual-review/tasks`。
- 动作使用子资源或明确命令，例如 `/tickets/{id}/transition`。
- ID 使用路径参数，筛选使用 query 参数。
- 后续不兼容变更使用 `/api/v2`，不得破坏 `/api/v1`。

### 8.2 统一响应格式

成功：

```json
{
  "success": true,
  "data": {},
  "message": "ok",
  "request_id": "req_20260701_000001"
}
```

分页：

```json
{
  "success": true,
  "data": {
    "items": [],
    "total": 0,
    "page": 1,
    "page_size": 20
  },
  "message": "ok",
  "request_id": "req_20260701_000001"
}
```

错误：

```json
{
  "success": false,
  "error": {
    "code": "TICKET_STATUS_INVALID",
    "message": "当前状态不允许该流转",
    "details": {
      "from_status": "closed",
      "to_status": "parsed"
    }
  },
  "request_id": "req_20260701_000001"
}
```

### 8.3 HTTP 状态码

| 状态码 | 使用场景 |
| --- | --- |
| 200 | 查询成功、普通更新成功 |
| 201 | 创建成功 |
| 202 | 后台任务已接收 |
| 204 | 删除或取消成功且无需返回内容 |
| 400 | 参数格式错误或业务参数不合法 |
| 401 | 未登录或 token 失效 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 409 | 状态冲突、乐观锁冲突、唯一约束冲突 |
| 422 | Pydantic 参数校验失败 |
| 429 | 请求过于频繁 |
| 500 | 未预期系统错误 |
| 502 | 外部服务错误，如 AI/OSS/IMAP/SMTP |

### 8.4 错误码规范

错误码使用大写蛇形：

```text
AUTH_INVALID_CREDENTIALS
AUTH_TOKEN_EXPIRED
PERMISSION_DENIED
EMAIL_NOT_FOUND
EMAIL_DUPLICATED
THREAD_LOCKED
TICKET_NOT_FOUND
TICKET_VERSION_CONFLICT
TICKET_STATUS_INVALID
PARSE_LOW_CONFIDENCE
SN_NOT_FOUND
SN_CUSTOMER_MISMATCH
REPLY_REVIEW_REQUIRED
REPLY_SEND_DISABLED
OSS_UPLOAD_FAILED
AI_PROVIDER_FAILED
JOB_RUNNING
```

说明：当前回复发送实现主要通过 `reply_records.send_status` 和 `error_message` 表达发送中、已发送、发送失败等状态；上表中尚未由代码直接返回的错误码按接口规范预留处理。

### 8.5 具体接口清单

#### 8.5.1 认证

`POST /api/v1/auth/login`

- 请求：`username`, `password`
- 校验：用户名必填，密码必填。
- 业务：校验用户状态与密码，更新 `users.last_login_at`。
- 响应：`access_token`, `token_type`, `expires_in`, `user`, `roles`。
- 错误：`AUTH_INVALID_CREDENTIALS`、`USER_DISABLED`。

`GET /api/v1/auth/me`

- 返回当前用户、角色和权限。

#### 8.5.2 首页看板

`GET /api/v1/dashboard/summary`

- 参数：`date_from`, `date_to`。
- 返回：今日新邮件、待解析、待人工、追问中、异常、可导出、AI 低置信度数量。
- 数据来源：`emails`、`repair_tickets`、`manual_review_tasks`、`reply_records`、`ai_call_logs`。

#### 8.5.3 邮件中心

`GET /api/v1/emails`

- 参数：`page`, `page_size`, `keyword`, `from_address`, `intent_type`, `parse_status`, `thread_id`, `has_attachment`, `received_from`, `received_to`。
- 返回：邮件列表，包含主题、发件人、接收时间、线程、解析状态、意图、附件数量、是否重复。

`GET /api/v1/emails/{id}`

- 返回：邮件头、正文、HTML、清洗正文、最新回复段、附件、线程时间线、关联工单、解析结果。

`POST /api/v1/emails/{id}/reparse`

- 请求：`parser_types`, `force`, `reason`。
- 校验：邮件存在；已关闭工单不允许强制覆盖人工锁定字段。
- 业务：创建解析任务或同步重解析，写 `job_run_logs`。
- 响应：解析任务 ID 或最新 `parse_results`。

#### 8.5.4 邮件线程

`POST /api/v1/email-threads/{id}/merge`

- 请求：`target_thread_id`, `reason`。
- 业务：合并线程、迁移邮件、更新工单关联，写 `operation_logs`。
- 限制：`manual_locked=1` 时只有 `admin` 可操作。

`POST /api/v1/email-threads/{id}/split`

- 请求：`email_ids`, `new_thread_reason`。
- 业务：拆分指定邮件到新线程，重算 `email_count` 和 `latest_email_id`。

#### 8.5.5 工单中心

`GET /api/v1/tickets`

- 参数：`page`, `page_size`, `status`, `customer_keyword`, `sn`, `assigned_user_id`, `created_from`, `created_to`, `has_manual_task`。
- 返回：工单编号、状态、客户、联系人、明细数量、SN 校验结果、追问次数、负责人、更新时间。

`GET /api/v1/tickets/{id}`

- 返回：工单主信息、明细、来源邮件、线程邮件、解析候选、SN 校验、回复记录、状态日志、字段审计。

`PATCH /api/v1/tickets/{id}/fields`

- 请求：`version`, `fields`, `items`, `reason`。
- 校验：版本号必须匹配；字段名必须在白名单中；只读用户禁止操作。
- 业务：更新工单字段或明细；写 `field_audit_logs` 和 `operation_logs`；增加 `version`。
- 错误：`TICKET_VERSION_CONFLICT`、`PERMISSION_DENIED`。

`POST /api/v1/tickets/{id}/transition`

- 请求：`to_status_code`, `trigger_event`, `reason`。
- 校验：必须命中 `workflow_transitions` 启用规则。
- 业务：更新状态，写 `ticket_status_logs`，必要时创建通知。

#### 8.5.6 解析结果

`GET /api/v1/tickets/{id}/parse-results`

- 返回该工单关联邮件的解析候选。

`POST /api/v1/parse-results/{id}/apply`

- 请求：`apply_scope`, `version`, `reason`。
- 业务：将解析候选应用到工单，跳过人工锁定字段，写审计。
- 失败：字段冲突、版本冲突、工单已关闭。

#### 8.5.7 人工复核

`GET /api/v1/manual-review/tasks`

- 参数：`status`, `task_type`, `priority`, `assigned_user_id`, `page`, `page_size`。
- 返回：任务列表、触发原因、关联工单、关联邮件、创建时间。

`POST /api/v1/manual-review/tasks/{id}/claim`

- 业务：当前用户领取任务，状态变为 `processing`。

`POST /api/v1/manual-review/tasks/{id}/assign`

- 请求：`assigned_user_id`。
- 权限：`admin` 或指定管理角色。

`POST /api/v1/manual-review/tasks/{id}/resolve`

- 请求：`resolution`, `next_action`。
- 业务：完成任务，可触发状态流转、生成追问或关闭工单。

#### 8.5.8 自动回复

`POST /api/v1/replies/{ticket_id}/draft`

- 请求：`missing_fields`, `template_code`, `generate_source`。
- 业务：生成草稿，写 `reply_records`，创建待审核通知。

`PATCH /api/v1/replies/{reply_id}`

- 请求：`subject`, `draft_body`, `final_body`。
- 业务：人工改写草稿，写 `operation_logs`。

`POST /api/v1/replies/{reply_id}/approve-send`

- 请求：`final_body`, `review_comment`。
- 业务：审核通过后进入发送流程，记录 `send_status`、`smtp_message_id` 或失败原因；AI 自动发送仅在 `reply_send_mode=auto_send`、`AUTO_SEND_ENABLED=true` 且满足置信度、收件人和风险条件时触发。当前代码尚未补齐外发邮件入库归档。

`POST /api/v1/replies/{reply_id}/reject`

- 请求：`reason`。
- 业务：更新 `review_status=rejected`。

#### 8.5.9 基础资料

`POST /api/v1/master-data/sn-assets/import`

- 请求：multipart 文件。
- 业务：解析 CSV/XLS/XLSX，upsert `sn_assets`，写 `job_run_logs`。
- 响应：成功数、失败数、异常行列表。

`GET /api/v1/master-data/sn-assets`

- 参数：`sn`, `customer_keyword`, `material_keyword`, `asset_status`。

`POST /api/v1/master-data/board-cards/import`

- 业务同 SN 导入，目标表 `board_cards`。

#### 8.5.10 AI 日志与通知

`GET /api/v1/ai-logs`

- 参数：`ticket_id`, `email_id`, `call_type`, `status`, `created_from`, `created_to`。
- 返回 AI 调用摘要，不返回完整 prompt 和完整正文。

`GET /api/v1/notifications`

- 返回当前用户或角色的站内通知。

`GET /api/v1/notifications/stream`

- SSE 推送；不可用时前端每 30 到 60 秒轮询 `/notifications`。

`POST /api/v1/notifications/{id}/read`

- 标记已读。

## 9. 前端开发设计

### 9.1 前端技术栈

- React 18+
- TypeScript
- Vite
- Ant Design
- React Router
- TanStack Query
- Zustand
- ESLint + Prettier
- dayjs

### 9.2 页面布局规范

控制台采用内部运营系统布局：

- 左侧固定导航。
- 顶部显示系统名、当前用户、通知入口、环境标识。
- 主区域以表格、筛选器、详情抽屉、分屏工作台为主。
- 不做营销式首页，不使用大面积装饰图。

主要导航：

```text
首页看板
邮件中心
工单中心
人工复核
自动回复审核
基础资料
AI 日志
系统配置
```

### 9.3 UI 规范

色彩：

| 用途 | 色值 |
| --- | --- |
| 主色 | `#1677ff` |
| 成功 | `#52c41a` |
| 警告 | `#faad14` |
| 错误 | `#ff4d4f` |
| 信息 | `#13c2c2` |
| 页面背景 | `#f5f7fa` |
| 主文本 | `#1f2329` |
| 次文本 | `#646a73` |
| 边框 | `#d9d9d9` |

排版：

- 页面标题：20px，600。
- 卡片标题：16px，600。
- 正文：14px。
- 辅助说明：12px。
- 表格密度默认 `middle`，高频工作页面允许 `small`。

状态标签：

| 状态 | 展示 |
| --- | --- |
| `new_email` | 蓝色，邮件已入库 |
| `parsed` | 青色，已解析 |
| `need_customer_info` | 橙色，待客户补充 |
| `auto_replied` | 紫色，已追问 |
| `manual_review` | 红色，人工复核 |
| `ready_for_export` | 绿色，可导出 |
| `error` | 红色，异常 |
| `closed` | 灰色，已关闭 |

响应式：

- 桌面端大于等于 1200px 使用左侧导航和详情抽屉。
- 平板 768px 到 1199px 保留导航折叠能力。
- 小于 768px 时列表优先展示核心字段，详情进入独立页面或全屏抽屉。

### 9.4 页面细节

#### 9.4.1 首页看板

组件：

- 指标卡：今日新邮件、待解析、待人工、追问中、异常、可导出、AI 低置信度。
- 最近异常任务表。
- 待审核回复表。
- 人工复核优先级队列。

交互：

- 点击指标跳转对应列表并带筛选参数。
- 异常任务支持查看详情。
- 通知入口显示未读数量。

#### 9.4.2 邮件中心

列表字段：

- 主题、发件人、接收时间、意图、解析状态、线程、附件数量、重复标记。

筛选：

- 关键词、发件人、意图、解析状态、时间范围、是否有附件。

详情：

- 原文信息、清洗正文、HTML 预览、邮件头、附件、线程时间线、关联工单、解析候选。

操作：

- 重新解析。
- 查看线程。
- 人工关联工单。
- 下载附件。

#### 9.4.3 工单中心

列表字段：

- 工单号、状态、客户、联系人、明细数量、SN 校验、追问次数、负责人、更新时间。

详情分区：

- 基础信息。
- 板卡/SN 明细。
- 来源邮件。
- 解析候选。
- SN 校验结果。
- 回复记录。
- 状态日志。
- 字段审计。

操作：

- 修改字段。
- 应用解析结果。
- 触发状态流转。
- 生成追问草稿。
- 转人工。
- 关闭工单。

#### 9.4.4 人工复核队列

视图：

- 按任务类型和优先级筛选。
- 表格显示任务、工单、客户、触发原因、状态、负责人、创建时间。

操作：

- 领取。
- 分配。
- 查看工单。
- 修改字段。
- 生成追问。
- 完成复核。

#### 9.4.5 自动回复审核

内容：

- 缺失字段。
- 追问轮次。
- 模板版本。
- AI 生成草稿。
- 收件人、抄送。
- 发送风险提示。

操作：

- 批准发送。
- 驳回。
- 人工改写。
- 查看关联邮件和工单。

#### 9.4.6 基础资料管理

页面：

- SN/资产库。
- 板卡/物料寄北京规则。

功能：

- 导入文件。
- 查询。
- 查看异常行。
- 查看导入来源。
- 导出当前筛选结果。

#### 9.4.7 AI 日志

功能：

- 按工单、邮件、调用类型、状态、时间筛选。
- 展示模型、提示词版本、输入摘要、输出摘要、关键结构化结果、置信度、耗时、日志文件定位。
- 不展示完整敏感正文，除非后续增加管理员审计能力。

#### 9.4.8 系统配置

功能：

- 查看当前 OSS、AI Provider、追问阈值、自动发送开关、角色权限、话术模板。
- 一期对敏感配置只展示脱敏值。
- 修改配置必须记录操作日志。

### 9.5 前端工程规范

目录：

```text
frontend/src/
  api/
    client.ts
    auth.ts
    emails.ts
    tickets.ts
    replies.ts
  components/
    StatusTag/
    DataTable/
    DetailDrawer/
    AuditTimeline/
  layouts/
    AppLayout.tsx
  pages/
  routes/
  stores/
  types/
  utils/
  styles/
```

规范：

- 页面组件使用 PascalCase。
- hooks 使用 `useXxx`。
- API 方法使用资源加动作，例如 `getTicketDetail`。
- 类型定义与后端 DTO 对齐。
- 服务端数据使用 TanStack Query，不放入 Zustand。
- 用户信息、侧边栏折叠、全局筛选偏好放入 Zustand。
- 表单提交必须显示 loading，成功后刷新 query，失败展示后端错误 message。
- 所有 destructive 操作必须二次确认。

## 10. 测试与验收标准

### 10.1 测试分层

后端单元测试：

- 主题归一化。
- Message-ID hash。
- 邮件头解析。
- 回复链识别。
- 模板字段解析。
- SN 校验。
- 状态机流转。
- OSS key 生成。
- API 参数校验。

后端集成测试：

- IMAP mock 拉取。
- OSS mock 上传。
- AI Provider mock。
- SMTP mock。
- 工单生成。
- 人工复核创建。
- 回复草稿生成和审核。

前端测试：

- 登录。
- 列表筛选和分页。
- 工单详情渲染。
- 字段修改表单。
- 回复审核。
- 人工复核流程。

回归测试：

- 标准完整报修邮件自动生成工单。
- 缺少 SN 的邮件生成追问草稿。
- 错误 SN 进入人工复核。
- 客户回复补充信息合并同一工单。
- 重复邮件不重复入库。
- 历史回复链只解析最新有效内容。
- 多板卡邮件生成多条明细。
- 附件承载主要信息时可解析附件。

### 10.2 模块验收标准

| 模块 | 验收标准 |
| --- | --- |
| 邮件接入 | 真实或模拟邮件可入库，EML 和附件进入 OSS，重复邮件不重复入库 |
| 线程归并 | 客户回复系统追问时落到原线程和原工单 |
| 解析 | 标准模板可解析，自由文本和附件能产生解析候选 |
| AI | 调用可追踪，低置信度进入人工，完整日志写 JSONL |
| SN 校验 | SN 存在、有效、客户匹配、物料匹配均可判断 |
| 工单 | 可生成、修改、状态流转、审计、关闭 |
| 自动回复 | 缺字段可生成草稿，默认不直接发送，审核流程可用 |
| 人工复核 | 任务可创建、领取、分配、完成，通知可见 |
| 前端 | 核心页面可完成日常操作，不需要直接访问数据库 |
| 部署 | Docker Compose 可启动，Nginx 可访问，日志可查看 |
| 文档 | README 和 AI 进度文档随功能同步更新 |

### 10.3 最终验收

最终演示必须覆盖：

1. 客户发送标准报修邮件，系统自动生成工单。
2. 客户邮件缺字段，系统生成追问草稿。
3. 客户回复补充信息，系统合并到同一工单。
4. SN 错误或低置信度，系统进入人工复核。
5. 人工修改字段后可查看字段审计。
6. 控制台可查看邮件原文、附件、AI 摘要、SN 校验、状态日志和回复记录。
7. 系统可通过 Docker Compose 启动，通过 Nginx 访问。
8. GitHub Actions 部署流程和日志查看方式有文档说明。

## 11. 阶段实施步骤

### 阶段 1：项目底座

- 对齐目录结构。
- 创建 README 和 AI 进度文档。
- 完成后端配置、数据库连接、日志、异常处理中间件。
- 完成前端脚手架、布局、登录页。
- 完成 Docker Compose、Nginx、GitHub Actions 初版。

### 阶段 2：数据库与基础 API

- 创建 26 张一期表迁移。
- 初始化状态、角色、管理员、回复模板。
- 实现认证、用户、看板基础 API。
- 实现统一响应、错误处理、分页。

### 阶段 3：邮件接入与归档

- 实现 IMAP 拉取、EML OSS 上传、附件 OSS 上传。
- 实现邮件列表、详情、附件查看。
- 实现线程归并初版。

### 阶段 4：解析、SN 校验与工单

- 实现规则解析、附件解析、解析结果入库。
- 实现 SN/板卡导入与校验。
- 实现工单生成、明细、状态流转和详情页。

### 阶段 5：AI、自动回复与人工复核

- 实现 AI Provider 与日志。
- 实现追问草稿、审核、发送开关。
- 实现人工复核队列、通知、字段审计。

### 阶段 6：测试、部署与交付

- 完成真实邮件回归。
- 完成前端 E2E。
- 完成 Docker/Nginx/GitHub Actions 部署验证。
- 完成 README、AI 进度文档、部署说明和交接材料。

## 12. 开发过程强制检查清单

每次开发结束前必须检查：

- 是否遵守 PRD 业务范围和一期边界。
- 数据库是否以 PRD 为主、一期最终版为字段细节。
- 是否新增或修改 Alembic migration。
- 是否写入或更新必要测试。
- 是否更新 README。
- 是否更新 `AI开发进度与任务跟踪.md`。
- 是否保持自动回复默认人工审核。
- 是否保证附件和 EML 不入库。
- 是否保证 AI 不覆盖人工锁定字段。
- 是否通过 Docker 或本地命令验证。
- 是否记录部署、日志和回滚相关说明。

## 13. 关键默认决策

- 一期数据库范围为 26 张表。
- 自动回复默认只生成草稿并人工审核。
- 真实自动发送由 `AUTO_SEND_ENABLED` 控制，默认关闭。
- SAP/IT 中转一期不真实对接，只保留导出边界。
- 邮箱账号配置一期放 `.env` 或服务配置文件。
- IMAP 拉取和基础资料导入一期由 `job_run_logs` 审计，不单独建设批次表。
- 客户信息从邮件解析和 SN 库获得，一期不建设客户主数据表。
- AI 详细日志写 JSONL 文件，数据库只保存摘要索引。
- 控制台定位为内部运营工具，优先效率和可追溯，不做营销式界面。
