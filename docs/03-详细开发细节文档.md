# 邮件报修自动化系统详细开发细节文档

> 最后更新时间：2026-07-13

## 0. 文档定位

本文档是具体功能开发、修改、重构和排查问题时的专项参考文档。开发指导文档只描述模块边界和原则，本文档描述各模块的接口、页面、业务流程、权限、异常和测试要点。

适用场景：

- 开发或修改某个业务模块。
- 调整 API、页面、权限、异常码。
- 排查邮件、工单、解析、人工复核、回复、通知等具体问题。
- 设计后期 LangGraph Agent 工作流落地方案。

何时查阅：

- 涉及具体功能实现前必须查阅。
- 修改 API 或前端页面前必须查阅。
- 修改状态流转、人工复核、AI 调用或回复发送前必须查阅。

维护规则：

- 本文档按模块维护细节，不重复数据库字段全量说明。
- 接口、页面、权限、异常码、测试点变更必须同步。
- LangGraph 相关内容必须明确标注为后期计划，不得伪装成现状。

## 1. 当前 API 模块总览

统一前缀：`/api/v1`

| 模块 | 前缀 | 当前用途 |
| --- | --- | --- |
| 认证 | `/auth` | 登录、当前用户、个人资料、修改密码 |
| 用户 | `/users` | 用户管理 |
| 首页 | `/dashboard` | 看板摘要 |
| 邮件 | `/emails` | 邮件列表、详情、入库、重解析 |
| 邮件线程 | `/email-threads` | 线程合并、拆分 |
| 工单 | `/tickets` | 工单列表、详情、编辑、导出、SN 校验、时间线 |
| 解析结果 | `/parse-results` | 解析结果采纳 |
| 人工复核 | `/manual-review` | 系统负责人任务池、处理、重解析；领取/分配/释放已停用 |
| 回复 | `/replies` | 回复列表、草稿、编辑、审核发送、拒绝 |
| 基础资料 | `/master-data` | SN 资产、板卡规则导入导出 |
| AI 日志 | `/ai-logs` | AI 调用摘要查询；管理员可读取受审计的 JSONL 明细 |
| 通知 | `/notifications` | 通知列表、标记已读、SSE 端点已注册（实时推送逻辑待增强） |
| 统计 | `/statistics` | 周期统计 |
| 系统 | `/system` | 系统信息、运行配置、话术模板 |
| 数据库浏览 | `/db-browser` | 白名单表结构和数据浏览 |

## 2. 认证与权限

后端：

- `backend/app/api/v1/auth.py`
- `backend/app/api/deps.py`
- `backend/app/core/security.py`
- `backend/app/services/users.py`

前端：

- `/login` → `Login.tsx`
- `/profile` → `ProfilePage.tsx`
- `frontend/src/stores/authStore.ts`
- `frontend/src/api/client.ts`

接口：

- `POST /auth/login`
- `GET /auth/me`
- `PATCH /auth/me/profile`
- `PATCH /auth/me/password`

权限原则：

- 登录成功返回 JWT 和角色。
- `get_current_user` 只允许 `status=active` 用户。
- `require_roles()` 中 `admin` 默认通过。
- 前端菜单隐藏不是安全边界。

异常要点：

- `AUTH_INVALID_CREDENTIALS`
- `AUTH_INVALID_TOKEN`
- `AUTH_FORBIDDEN`
- `USER_OLD_PASSWORD_INVALID`
- `USER_PASSWORD_REQUIRED`
- `USER_PASSWORD_TOO_LONG`

测试要点：

- 登录成功和失败。
- token 失效。
- 角色不足返回 403。
- 修改密码后旧密码失效。

## 3. 用户管理

后端：

- `backend/app/api/v1/users.py`
- `backend/app/services/users.py`
- `backend/app/models/identity.py`

前端：

- `/users`
- `frontend/src/pages/UsersPage.tsx`

接口：

- `GET /users`
- `POST /users`
- `PATCH /users/{user_id}`
- `PATCH /users/{user_id}/status`
- `PUT /users/{user_id}/roles`
- `POST /users/{user_id}/reset-password`
- `DELETE /users/{user_id}`

权限：

- 仅 `admin`。

业务规则：

- 角色只允许 `admin / supervisor / operator`。
- 用户名唯一。
- 邮箱唯一。
- 不能删除当前登录账号。
- 有业务引用的用户删除需谨慎，当前 service 会检查相关引用。

测试要点：

- 角色初始化缺失。
- 重复用户名/邮箱。
- operator 访问用户管理返回 403。
- 删除用户引用保护。

## 4. 首页看板与统计分析

后端：

- `backend/app/api/v1/dashboard.py`
- `backend/app/api/v1/statistics.py`

前端：

- `/`
- `/statistics`
- `Dashboard.tsx`
- `StatisticsPage.tsx`

接口：

- `GET /dashboard/summary`
- `GET /statistics/summary`

权限：

- 首页摘要需要登录。
- 统计分析需要 `supervisor` 或 `admin`。

统计内容：

- 邮件数量。
- 工单数量。
- 完成数量。
- 重解析数量。
- AI 成功率。
- 自动回复率。
- 人工介入率。
- 任务池数量。
- 状态分布。
- 用户处理量。
- 趋势数据。

测试要点：

- 日期范围非法返回 `STATISTICS_DATE_RANGE_INVALID`。
- operator 不应访问统计分析。

## 5. 邮件中心与邮件线程

后端：

- `backend/app/api/v1/emails.py`
- `backend/app/api/v1/email_threads.py`
- `backend/app/services/emails.py`
- `backend/app/services/mail_precheck.py`
- `backend/app/services/imap_fetcher.py`
- `backend/app/services/eml.py`
- `backend/app/services/storage.py`
- `backend/app/services/attachment_parser.py`
- `backend/app/services/parser.py`
- `backend/app/models/mail.py`
- `backend/app/models/mail_fetch.py`

前端：

- `/emails`
- `EmailsPage.tsx`

接口：

- `GET /emails`
- `GET /emails/export`
- `GET /emails/{email_id}`
- `GET /emails/{email_id}/flow-trace`
- `GET /emails/{email_id}/raw-eml-url`
- `GET /emails/attachments/{attachment_id}/download-url`
- `POST /emails/ingest`
- `POST /emails/ingest/jobs`
- `POST /emails/ingest-eml`
- `POST /emails/ingest-eml/jobs`
- `POST /emails/fetch-now`
- `POST /emails/fetch/jobs`
- `POST /emails/{email_id}/reparse`
- `POST /emails/{email_id}/reparse/jobs`
- `POST /email-threads/{thread_id}/merge`
- `POST /email-threads/{thread_id}/split`

当前流程：

1. 手工 JSON、EML 文件或 IMAP 形成统一 `EmailIngestRequest`；raw EML 计算 SHA-256。
2. IMAP 入口先按 `mailbox_account + folder_name + imap_uid` 查 `mail_fetch_records`；全部入口再规范化 Message-ID 并查 `emails.message_id`。缺失 Message-ID 时由 raw hash 生成稳定替代值。
3. 归档前剔除装饰性 inline 图片，并执行完整规则候选解析：分类、字段、明细、缺失、冲突、置信度和证据一次生成。只有明确无关且无业务/回复/有效附件信号才跳过。
4. 通过预检后上传 raw EML 和全部有效附件到 OSS；任一失败不创建正式邮件。装饰图片不创建附件对象，但原始 EML 保持原样归档。
5. 全部对象成功后在正式事务写入线程、邮件、附件和预规则 `parse_results`；异步入口同时写初次 job 并复用该规则记录，显式“重新解析”才生成新规则记录。同步兼容入口调用相同 service。
6. DeepSeek 汇总正文、规则上下文和附件解析结果生成邮件级候选，随后自动采纳或转人工。

EML 前端导入规则：

- `EmailsPage` 固定调用 `POST /emails/ingest-eml/jobs`，上传完成后立即登记 job，由全局任务监视器轮询；同步接口保留给兼容和诊断使用。
- 导入请求超时为 600 秒；Nginx 关闭 API 请求缓冲并允许大于应用单附件限制的 EML 请求到达后端，由业务层按附件规则判定。
- 异步 worker 对每个附件阶段独立提交。重试时复用已归档 OSS 对象，并跳过 `parsed`、`skipped_decorative`、`unsupported` 终态，避免重复调用模型。
- MySQL 模型启用 `eager_defaults`，确保 flush 后可直接读取 `created_at/updated_at` 等服务端默认值，不在异步上下文触发隐式 IO。
- 重新解析从原始 `text_body/html_body` 重建最新正文和历史对话，禁止使用上次已裁剪的 `clean_body` 覆盖历史 SN 等上下文。

手工 `/emails/ingest` 的 `attachments` 元素支持：

```json
{
  "file_name": "fault-report.pdf",
  "content_type": "application/pdf",
  "file_size": 1024,
  "content_base64": "<base64>"
}
```

导入结果可包含 `duplicate`、`skipped`、`email` 和 `precheck`；重复与高置信无关邮件必须在前端给出明确提示。

当前数据模型：

- 使用 `emails`，不是 `business_emails`。
- `mail_fetch_records` 已用于 IMAP UID 幂等和拉取审计。

异常要点：

- `EMAIL_NOT_FOUND`
- `EMAIL_THREAD_NOT_FOUND`
- `EMAIL_THREAD_SAME_TARGET`
- `EMAIL_THREAD_SPLIT_EMPTY`
- `EML_FILE_REQUIRED`
- `EML_FILE_EMPTY`
- `ATTACHMENT_CONTENT_INVALID`
- `RAW_EML_NOT_ARCHIVED`
- `ATTACHMENT_NOT_ARCHIVED`
- `OSS_OBJECT_NOT_FOUND`

现行一致性规则：三个入口共用强归档服务；OSS 未配置返回 `OSS_NOT_CONFIGURED`，原件或任一附件失败返回 `OSS_ARCHIVAL_FAILED`，均不得创建正式邮件。IMAP 失败 UID 写入退避时间并由后续调度重试。

测试要点：

- 重复 message_id 不重复入库。
- `In-Reply-To` 命中线程。
- 重复和高置信无关邮件不产生 OSS 对象或正式邮件。
- 缺失 Message-ID 的同一 EML 通过 raw hash 去重。
- raw EML 与每个正式附件均有关联的成功 OSS 对象。
- AI 不可用时生成人工复核。

## 6. AI 解析与解析结果采纳

后端：

- `backend/app/services/ai.py`
- `backend/app/integrations/ai_provider.py`
- `backend/app/api/v1/parse_results.py`
- `backend/app/services/tickets.py`
- `backend/app/models/parsing.py`
- `backend/app/models/logs.py`

前端：

- 邮件详情解析结果。
- 工单详情解析证据。
- `/ai-logs` → `AiLogsPage.tsx`

接口：

- `POST /parse-results/{parse_result_id}/apply`
- `POST /tickets/parse-results/{parse_result_id}/apply`
- `GET /ai-logs`

AI 调用规则：

- DeepSeek 是邮件级文本 Provider，只接收正文、规则候选和附件已经抽取的文本/JSON，不接收附件二进制。
- Qwen 是附件级 Provider：文本/表格类附件先本地抽取后调用 Qwen JSON；图片和无文本 PDF 使用 OSS 短期预签名 URL 调 Qwen VL。
- AI 必须输出 JSON。
- 使用 Pydantic 模型校验输出。
- 低置信、缺字段、冲突、AI 不可用进入人工复核。
- 自动采纳阈值由 `AUTO_APPLY_MIN_CONFIDENCE` 控制，默认 0.85；还必须满足无缺失、无冲突、关键附件成功且 SN 校验全部通过，否则创建或复用人工任务。
- 已关闭工单线程收到新邮件时不得自动修改关闭工单，必须创建人工判断任务。
- 数据库 `ai_call_logs` 只写摘要、错误分类、重试和 token usage。JSONL 保存完整文本 Prompt、请求、原始响应和解析结果，管理员通过 `GET /ai-logs/{id}/detail` 查看且产生操作审计；key、认证头、签名参数、data/base64 和二进制必须脱敏，默认保留 30 天。

统一附件解析矩阵：

| 类型 | 本地处理 | Qwen 输入 | 限制和结果 |
| --- | --- | --- | --- |
| `txt` | 多编码文本解码 | 截断后的文本 | 最大模型输入由 `ATTACHMENT_TEXT_MAX_CHARS` 控制 |
| `csv` | 最多抽取前 200 行 | 表格文本 | 长文本记录 `TEXT_TRUNCATED_FOR_MODEL_INPUT` |
| `html` | BeautifulSoup 转纯文本 | 截断后的文本 | 不执行脚本 |
| `docx` | 从 OOXML 提取段落 | 段落文本 | 损坏文件转人工 |
| `xlsx` | 最多 10 个 sheet、每表 200 行、40 列 | 表格文本 | 只读加载 |
| `image/*` | 不做本地 OCR | 30 分钟预签名 URL | Qwen VL |
| `pdf` | 前 15 页抽文本 | 文本或预签名预览 URL | 超页标记 `truncated=true` |

图片与 PDF 补充规则：

- 无 `Content-Disposition` 但带 `Content-ID` 的 MIME 图片按 inline 处理；低于配置尺寸/字节阈值的签名、图标等资源写 `skipped_decorative`，不调用 Qwen。
- 文本 PDF 使用本地抽取文本后调用 Qwen 文本模型；扫描 PDF 使用 PyMuPDF 渲染受限页数，再以临时数据输入调用 Qwen VL。
- 预签名 URL 查询串、渲染 data/base64 和附件二进制不得进入任何日志、浏览器控制台或测试快照；文本附件送模后的语义输入允许进入受限 AI JSONL。

2026-07-15 指定样本验收：原始 MIME 共识别 12 个附件，现行链在 OSS 前剔除 7 个装饰 inline 资源，仅创建 5 个业务附件与对应 OSS 对象；原始 EML 仍完整归档。5 个业务附件最终均由 Qwen 解析成功，预规则结果由后续 `email_parse` job 复用，流程终点形成待审核回复草稿。

- 单附件超过 50MB 仍保留 OSS 对象，但不自动解析，写 `needs_manual_review/FILE_TOO_LARGE_FOR_AUTO_PARSE`。
- 不支持类型写 `unsupported/UNSUPPORTED_FILE_TYPE`。
- Qwen 失败仅影响对应附件，保留已抽取文本并转人工；DeepSeek 邮件级解析继续执行。
- Qwen 响应对常见 JSON 形状漂移执行受控归一化；若仍不满足 schema，`ai_call_logs` 与内部 JSONL 保留脱敏后的完整语义请求、原始模型响应和错误分类，便于提示词调优。
- 附件 JSON 固定包含 `file_type`、`summary`、`key_points`、`extracted_fields`、`extracted_items`、`raw_text`、`warnings`、`truncated`。

解析结果状态：

- 规则候选：`candidate_only`
- 待处理：`pending`
- 自动采纳：`auto_applied`
- 人工采纳：`manually_applied`
- 部分采纳：`partially_applied`
- 拒绝：`rejected`
- 需人工：`needs_manual_review`
- 无关自动跳过：`auto_skipped`

异常要点：

- `PARSE_RESULT_NOT_FOUND`
- `PARSE_RESULT_TICKET_NOT_FOUND`
- `PARSE_RESULT_ACTION_INVALID`

测试要点：

- AI 低置信状态。
- AI 不可用转人工。
- 采纳创建工单。
- 拒绝不改写工单字段。
- `field_audit_logs` 写入。

## 7. 工单中心与状态流转

后端：

- `backend/app/api/v1/tickets.py`
- `backend/app/services/tickets.py`
- `backend/app/services/workflow.py`
- `backend/app/models/tickets.py`
- `backend/app/models/workflow.py`

前端：

- `/tickets`
- `TicketsPage.tsx`

接口：

- `GET /tickets`
- `GET /tickets/export`
- `POST /tickets/export-selected`
- `GET /tickets/{ticket_id}`
- `PATCH /tickets/{ticket_id}/fields`
- `PATCH /tickets/{ticket_id}/items`
- `POST /tickets/{ticket_id}/transition`
- `POST /tickets/{ticket_id}/validate-sn`
- `GET /tickets/{ticket_id}/parse-results`
- `GET /tickets/{ticket_id}/email-timeline`
- `GET /tickets/{ticket_id}/attachments`
- `GET /tickets/{ticket_id}/field-evidence`

权限：

- 状态流转接口需要 `supervisor` 或 `admin`。
- 其他接口当前按登录用户开放，后续行级权限需设计后再收紧。

状态流转：

- `transition_ticket()` 校验目标状态存在、流转规则启用、工单未关闭。
- 进入 `manual_review` 时自动创建人工复核任务。
- 直接修改状态字段是不允许的，除工单创建初始化外必须走服务。

当前 `ready_for_export`：

- `validate_ticket_sn()` 仅完成 SN 核心校验；SN 通过不会改变为 `ready_for_export`。
- 不要求客户确认收到维修后设备。

异常要点：

- `TICKET_NOT_FOUND`
- `TICKET_VERSION_CONFLICT`
- `TICKET_FIELD_NOT_ALLOWED:*`
- `TICKET_ITEM_FIELD_NOT_ALLOWED:*`
- `WORKFLOW_TRANSITION_NOT_ALLOWED`
- `WORKFLOW_STATUS_NOT_FOUND`
- `TICKET_ALREADY_CLOSED`
- `EXPORT_SELECTION_REQUIRED`
- `EXPORT_SELECTION_EMPTY`

测试要点：

- 字段编辑版本冲突。
- 非法字段拒绝。
- 非法状态流转拒绝。
- SN 校验失败进入人工复核。
- 导出选择为空返回错误。

## 8. SN 校验与基础资料

后端：

- `backend/app/api/v1/master_data.py`
- `backend/app/services/master_data.py`
- `backend/app/models/master_data.py`
- `backend/app/services/tickets.py`

前端：

- `/master-data`
- `MasterDataPage.tsx`

接口：

- `GET /master-data/sn-assets`
- `GET /master-data/sn-assets/template`
- `GET /master-data/sn-assets/export`
- `POST /master-data/sn-assets/export-selected`
- `POST /master-data/sn-assets/import`
- `POST /master-data/sn-assets/import-file`
- `GET /master-data/board-cards`
- `GET /master-data/board-cards/template`
- `GET /master-data/board-cards/export`
- `POST /master-data/board-cards/export-selected`
- `POST /master-data/board-cards/import`
- `POST /master-data/board-cards/import-file`

权限：

- 查询、模板、导出：`supervisor` 或 `admin`。
- 导入：`admin`。

SN 校验规则：

- SN 缺失：失败。
- SN 不存在：失败。
- 资产状态非 `valid`：失败。
- 客户不匹配：警告。
- 物料不匹配：警告。
- 通过时可回填物料编码和名称。

异常要点：

- `CSV_HEADER_REQUIRED`
- `CSV_VALIDATION_FAILED`
- `XLSX_HEADER_REQUIRED`
- `XLSX_VALIDATION_FAILED`
- `XLSX_INVALID_FILE`

测试要点：

- CSV/XLSX 模板表头。
- 导入创建和更新。
- SN 大小写归一。
- admin 与 supervisor 权限差异。

## 9. 人工复核

后端：

- `backend/app/api/v1/manual_review.py`
- `backend/app/services/manual_review.py`
- `backend/app/services/workflow.py`
- `backend/app/models/review.py`

前端：

- `/manual-review`
- `ManualReviewPage.tsx`

接口：

- `GET /manual-review/tasks`
- `GET /manual-review/tasks/{task_id}`
- `POST /manual-review/tasks/{task_id}/claim`（deprecated，返回 410）
- `POST /manual-review/tasks/{task_id}/assign`
- `POST /manual-review/tasks/{task_id}/release`
- `POST /manual-review/tasks/{task_id}/resolve`
- `POST /manual-review/tasks/{task_id}/reparse`

权限：

- 任务列表、详情、处理、重解析：`operator`、`supervisor`、`admin`。
- 领取、释放、分配：统一返回 HTTP 410 `TASK_ASSIGNMENT_DISABLED`。

进入人工复核的典型原因：

- AI 低置信。
- 字段缺失。
- 字段冲突。
- SN 异常。
- 系统错误。
- 回复发送失败。
- 追问次数达到上限。
- 后续 LangGraph 中断/人工 resume。

处理动作：

- `transition_ready_for_export`
- `wait_customer_info`
- `generate_followup`
- `reparse`
- `close_ticket`
- `keep_manual_review`

异常要点：

- `MANUAL_TASK_NOT_FOUND`
- `MANUAL_TASK_NOT_CLAIMABLE`
- `MANUAL_TASK_NOT_RELEASABLE`
- `MANUAL_TASK_ASSIGNED_TO_OTHER`
- `MANUAL_TASK_CLAIMED_BY_OTHER`
- `MANUAL_TASK_ALREADY_RESOLVED`
- `MANUAL_TASK_NEXT_ACTION_INVALID`

测试要点：

- operator 可领取未分配任务。
- operator 不可领取分配给他人的任务。
- supervisor 可分配任务。
- 已解决任务不可重复处理。

## 10. 回复管理与话术模板

后端：

- `backend/app/api/v1/replies.py`
- `backend/app/api/v1/system.py`
- `backend/app/services/replies.py`
- `backend/app/services/ai.py`
- `backend/app/models/replies.py`

前端：

- `/replies`
- `/system`
- `RepliesPage.tsx`
- `SystemPage.tsx`

接口：

- `GET /replies`
- `POST /replies/{ticket_id}/draft`
- `PATCH /replies/{reply_id}`
- `POST /replies/{reply_id}/approve-send`
- `POST /replies/{reply_id}/reject`
- `GET /system/reply-templates`
- `POST /system/reply-templates`
- `PATCH /system/reply-templates/{template_id}`
- `DELETE /system/reply-templates/{template_id}`

权限：

- 草稿和编辑：`operator`、`supervisor`、`admin`。
- 审核通过/拒绝：`supervisor`、`admin`。
- 话术模板：`supervisor`、`admin`。

发送规则：

- 默认 `AUTO_SEND_ENABLED=false`。
- 默认 `REPLY_SEND_MODE=human_review`。
- 自动发送必须同时满足配置、收件人有效、置信度阈值、风险等级要求。
- 发送失败创建人工复核任务。
- 发送成功归档出站邮件到 `emails` 并建立 `email_ticket_links`。
- 草稿创建不代表已发送：不可自动发送时工单进入或保持 `manual_review`，只创建 `reply_review`；追问上限只创建 `followup_limit`。
- 非回执邮件仅在 SMTP 成功后通过 `reply_sent` 从 `need_customer_info/manual_review` 进入 `auto_replied`；白名单、出站归档或 SMTP 失败均进入或保持人工态。

异常要点：

- `REPLY_NOT_FOUND`
- `REPLY_ALREADY_APPROVED`
- `REPLY_TEMPLATE_NOT_FOUND`
- `FOLLOWUP_LIMIT_EXCEEDED`
- `REPLY_TEMPLATE_ALREADY_EXISTS`
- `REPLY_TEMPLATE_IN_USE`
- `REPLY_FIELD_NOT_ALLOWED:*`

测试要点：

- 模板缺失。
- 追问轮次上限。
- 审核权限。
- SMTP 未配置时转人工。
- SMTP 发送前及实际网络调用前均解析完整 `To + Cc` 收件人集合；只有非空集合严格为 `SMTP_RECIPIENT_WHITELIST` 子集时才允许发送。

## 11. 通知中心

后端：

- `backend/app/api/v1/notifications.py`
- `backend/app/services/audit.py`
- `backend/app/services/workflow.py`

前端：

- `/notifications` → `NotificationsPage.tsx`
- `/notification-center` → `NotificationCenterPage.tsx`
- 布局顶部通知入口。

接口：

- `GET /notifications`
- `POST /notifications/{notification_id}/read`
- `GET /notifications/stream`

规则：

- 通知可发给具体用户或角色。
- 用户只能标记自己可见的通知。
- 当前 SSE 端点存在，但完整实时推送闭环仍可继续增强。

测试要点：

- 未读筛选。
- 用户不可读无权限通知。
- 创建人工任务时生成通知。

## 12. 系统配置与数据库浏览

系统配置：

- `GET /system/info`
- `GET /system/config`
- `PATCH /system/config`

配置项：

- `auto_send_enabled`
- `reply_send_mode`
- `auto_send_min_confidence`
- `confidence_threshold`
- `max_follow_up`

权限：

- `supervisor` 或 `admin`。

数据库浏览：

- `GET /db-browser/tables`
- `GET /db-browser/tables/{table_name}/rows`

前端：`/db-browser` → `DbBrowser.tsx`

权限：

- 仅 `admin`。

规则：

- 只允许白名单表。
- 只读浏览，不提供写入。

## 13. 日志、异常、后台任务

日志表：

- `operation_logs`
- `system_event_logs`
- `job_run_logs`
- `ai_call_logs`
- `ticket_status_logs`
- `field_audit_logs`

日志职责：

- `ai_call_logs` 记录每次模型调用状态、关键结果、重试、耗时和 token usage，不保存完整请求响应。
- `operation_logs` 只记录用户或管理员操作；`ticket_status_logs` 只记录工单状态迁移；`field_audit_logs` 只记录字段修改。
- `system_event_logs` 记录邮件阶段、外部依赖、重试和技术异常；`job_run_logs` 负责任务队列状态、进度、锁和结果引用。
- 请求 middleware 生成或透传安全的 `X-Correlation-ID`，生命周期接口以 correlation、email、ticket 和 job 关联已有记录。

异常处理：

- FastAPI 全局处理 HTTPException、RequestValidationError、未捕获异常。
- 前端 `apiErrorMessage()` 将错误码映射为用户友好文案。

后台任务现状：

- `job_run_logs` 已复用为可恢复任务队列，状态为 `queued/running/retry_wait/success/needs_manual_review/failed/cancelled`。
- APScheduler 依赖已安装。
- FastAPI lifespan 已初始化 `AsyncIOScheduler`，按配置注册 IMAP 拉取、系统维护和异步 worker。
- IMAP poller 已实现，默认 `UNSEEN + BODY.PEEK[]`、只读选择文件夹、不移动邮件、不标记已读。
- IMAP 调度使用 MySQL `GET_LOCK` 防止多实例同时执行，每封邮件独立提交；失败 UID 按 5、15、60、180、720 分钟退避。
- Worker 使用 `FOR UPDATE SKIP LOCKED` 领取任务并恢复超过 15 分钟的陈旧锁；同步接口默认保留，按功能开关逐项启用异步版本。

异步及运行状态接口：

- `GET /jobs`、`GET /jobs/{job_id}`、`GET /jobs/{job_id}/download-url`
- `POST /exports/jobs`
- `POST /replies/{reply_id}/approve-send/jobs`
- `POST /master-data/sn-assets/import-file/jobs`
- `POST /master-data/board-cards/import-file/jobs`
- `GET /system/runtime-status`

生命周期接口：

- `GET /emails/{email_id}/flow-trace` 聚合邮件拉取、归档、解析、AI、工单、人工、回复和任务记录。
- `GET /tickets/{ticket_id}/timeline` 聚合工单邮件关联、SN 校验、人工操作、字段审计、状态和回复记录。
- `POST /tickets/{ticket_id}/confirm-export` 已废弃并返回 410；收货事件只在 RMA 已成功发送时关闭工单。

## 14. 后期 LangGraph Agent 工作流

定位：

- LangGraph 是后期编排层，负责节点编排、条件路由、人工暂停/恢复、失败恢复、执行追踪。
- LangGraph 不直接替代 FastAPI service、数据库事务、RBAC、幂等控制和状态机合法性判断。

推荐目录：

```text
backend/app/graphs/
  email_repair_graph.py
  state.py
  nodes/
  edges.py
  persistence.py
  prompts/
```

触发入口：

- P0 可从手工入库邮件后触发。
- 后续 IMAP poller 可触发批量单封邮件 Graph。

State 建议：

- `graph_run_id`
- `email_id`
- `thread_id`
- `ticket_id`
- `raw_email_summary`
- `attachments`
- `rule_result`
- `ai_result`
- `validation_result`
- `action_plan`
- `manual_review_required`
- `manual_review_reason`
- `reply_draft_id`
- `errors`
- `retry_count`

节点建议：

- `body_preprocess_node`
- `rough_classify_node`
- `attachment_detect_node`
- `attachment_extract_node`
- `ocr_parse_node`
- `vl_parse_node`
- `ai_full_parse_node`
- `schema_validate_node`
- `business_validate_node`
- `ticket_prepare_node`
- `human_review_interrupt_node`
- `reply_draft_node`
- `finalize_node`

Conditional Edge：

- 明确无关邮件 -> finalize。
- 普通文本邮件 -> AI 全量解析。
- 有附件 -> 附件抽取。
- 图片/扫描件 -> OCR/VL。
- JSON 校验失败 -> retry 或人工复核。
- 低置信/冲突/缺字段 -> 人工复核或追问草稿。
- 高置信完整 -> service 层采纳。

人工 interrupt/resume：

- interrupt 时创建 `manual_review_tasks`。
- resume payload 必须 schema 校验。
- resume 后仍通过 service 执行状态流转或字段更新。

日志和持久化：

- Graph 运行先复用 `job_run_logs`，节点技术事件复用 `system_event_logs`，模型调用复用 `ai_call_logs`。
- 只有 interrupt/resume 无法由现有任务状态表达时才评估 checkpoint 存储，不预建低利用表。
- 节点日志只存摘要和引用，不存大文本、附件全文、密钥。

重试：

- LLM JSON 格式错误可重试。
- LLM 超时可有限重试。
- OCR/VL 调用失败可有限重试。
- 加密压缩包、超过附件上限、状态非法不盲目重试，直接转人工。

测试要点：

- 单节点单元测试。
- 条件分支测试。
- LLM mock。
- 工具调用 mock。
- interrupt/resume。
- 数据库落库。
- FastAPI 触发接口联调。

## 当前前端与外部集成基线

- 邮件页支持 EML 导入、手工附件选择、按筛选条件导出 `.xlsx`、raw EML 下载、附件下载及解析文本/JSON 展示。
- 邮件正文和附件支持安全预览：图片原生预览，PDF 由 PyMuPDF 渲染前 5 页，HTML/TXT/CSV 安全内容预览，DOCX/XLSX 展示抽取文本和 JSON；远程跟踪图片、脚本和事件属性被移除。
- 工单页支持已选导出，导出包含附件摘要、缺失/冲突字段、SN 校验和回复状态；工单详情可下载和查看附件解析结果。
- 人工复核页可查看和下载附件、查看解析文本/JSON，并保留重新解析、SN 校验和追问入口。
- 主数据页保留 SN/板卡模板下载、文件导入、筛选导出和已选导出。
- 系统页分别显示 DeepSeek 文本 AI、Qwen 附件 AI 及 Relay 开关状态。
- SQL Server 中转占位服务为 `external_relay.py`；开关关闭或配置不完整时不得发起 ODBC/网络请求，原始 AI 结果推送接口仅返回 deprecated。

## 16. SN、安全闸门、RMA 与 SQL Server API

- `POST /tickets/{id}/validate-sn`：标准化 SN，校验格式、重复、存在、状态、客户、物料并保存保修证据；失败创建 `sn_validation_failed`。
- `GET /tickets/{id}/export-safety`：只读查看当前完整安全报告，不改变状态。
- `POST /tickets/{id}/validate-export`：先执行或复用新鲜 SN 结果；失败立即停止。全部通过后生成快照/哈希并进入 `ready_for_export`。
- 通用 `/transition` 对 `ready_for_export` 返回 `EXPORT_SAFETY_GATE_REQUIRED`。
- `PATCH /tickets/{id}/owner`：仅 admin 用于负责人纠错，不是正常分配入口。
- `GET /system/integrations/sqlserver/status`：只返回 `disabled/misconfigured/configured` 和缺失项，不返回密码。
- `POST /system/rma-test/preflight`：仅 admin，纯离线生成、解析测试 PDF/MIME；返回精确地址、附件哈希和失败原因，绝不发送。

RMA 任务执行前再次比较 `ticket_version + safety_check_hash + sn_validation_hash`；任一变化都应标记旧任务 `superseded`。RMA 邮件仅允许一个 To，Cc/Bcc 为空，归档 EML 必须含唯一 PDF 附件和对应 `EmailAttachment`。
